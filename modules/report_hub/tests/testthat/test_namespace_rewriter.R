# Report Hub -- Namespace Rewriter Tests
# DEPRECATED: The namespace rewriter (02_namespace_rewriter.R) is no longer used.
# The iframe approach eliminates the need for namespace rewriting entirely.

test_that("namespace rewriting is no longer needed (iframe approach)", {
  skip("Namespace rewriting replaced by iframe isolation")
})
