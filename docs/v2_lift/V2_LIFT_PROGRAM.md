# V2 Lift Programme — Master Tracker

**Purpose:** bring the analytical modules (maxdiff, conjoint, pricing, keydriver, catdriver, segment) to tabs-v2-level solidity and reporting, with tabs integration where the data shape allows. This is the single coordinating document: every review and implementation session reads it first and updates its row before ending.
**Started:** 2026-07-10. Owner: Duncan. Process author: Fable review session (maxdiff).
**Companion docs:** `modules/tabs/docs/V2_MIGRATION_PLAN.md` (the socket contract + migration mechanics — the technical spine); per-module `{MODULE}_PRODUCTION_REVIEW_*.md` and `HANDOVER_{MODULE}_FOR_OPUS.md` in this folder.

---

## 1. Standing decisions (apply to every module unless a row says otherwise)

| # | Decision | Status |
|---|----------|--------|
| D1 | **Curated** content bar for v2 migration, not parity. Deliberate best-of into the interactive report; exhaustive diagnostics stay in the module's Excel deliverable. Every capability left behind is logged against V2_MIGRATION_PLAN §7's drop-list — a choice, never an accident. | Locked (plan rec, adopted 2026-07-10) |
| D2 | **Simulators stay standalone**, linked not embedded (pricing, conjoint, maxdiff). | Locked (plan rec, adopted 2026-07-10) |
| D3 | **Order: maxdiff first** (Duncan, 2026-07-10 — jumped the queue), then per review findings; default remainder segment → keydriver → catdriver → pricing → conjoint, re-orderable as reviews land. | Locked |
| D4 | **Correctness before lift.** Each module's engine/config fixes (its Phase 1) land before its reporting lift. No better report for wrong numbers. | Locked |
| D5 | **Honest-sig principle:** model *estimates* entering tabs or v2 sig machinery must carry their method; approximate/fallback estimators refuse export by default (override + prominent stamping). Mirrors the tracker aggregate-ingest honest-sig convention. | Locked |
| D6 | **Session pipeline per module** (§2). Fable for review + handover + pre-merge review of engine fixes; Opus 4.8 for implementation; Sonnet 5 optional for template/doc mechanical passes against a written spec. | Locked |

## 2. The pipeline each module goes through

1. **Fable review session** → `docs/v2_lift/{MODULE}_PRODUCTION_REVIEW_{date}.md`. Method (proven on maxdiff): four parallel independent readers — (a) statistical engine bug-hunt, (b) config/UX/doc-drift, (c) report gap-vs-v2 + report bugs, (d) tabs/v2 integration feasibility — plus a test-suite run and coordinator spot-verification of every CRITICAL claim. Findings carry file:line + severity; nothing unverified goes in unlabelled.
2. **Fable locks decisions + writes** `docs/v2_lift/HANDOVER_{MODULE}_FOR_OPUS.md` — explicit work orders with acceptance criteria, session split, branch names.
3. **Opus implementation session(s)**, one branch per session, suites green, no merge.
4. **Fable independent pre-merge review** of the engine-fix session (briefed as independent — no shared working notes with the fixing session).
5. **Duncan**: regenerate via `launch_turas()`, eyeball, merge, push. (Standing rule: sessions never headless-run pipelines on real projects or touch OneDrive.)
6. Session updates its row in §4 and appends to §5 log.

## 3. Programme-level work items

| ID | What | Who | Status |
|----|------|-----|--------|
| OPUS-0 | **Socket consolidation** (V2_MIGRATION_PLAN Phase-0 part 2): extract ONE Kish n_eff into `modules/shared/` (today: `modules/tabs/lib/weighting.R::calculate_effective_n` is canonical; duplicates in `modules/confidence/R/03_study_level.R`, dependency in `modules/tracker/lib/trend_significance.R`); ONE disclosure/min-base gate reusable by migrating modules; retire per-module `*_pins.js` onto shared TurasPins. Prerequisite for any Phase-4/v2 migration (not for correctness or tabs-exporter phases). **Handover DONE 2026-07-12: `docs/v2_lift/HANDOVER_OPUS0_FOR_OPUS.md` — authoritative over this row, which it corrects on three facts (tracker only *consumes* a precomputed `eff_n`, no duplicate to re-point; a FOURTH behaviourally-divergent Kish copy exists at `modules/maxdiff/R/utils.R:585`; per-module `*_pins.js` are thin wrappers already on shared TurasPins, so the real pins work is `</script>` island hardening, pulled in from catdriver B5).** Fable pre-merge review required. | Opus (Fable review after) | **DONE 2026-08-27** on `feature/opus0-socket-consolidation`, MERGED into main via the conjoint v2 branch (`f03ee8df`) and since pushed. W1 shared Kish n_eff, W2 shared `meets_min_base()`, W3 island hardening + inventory. Notes and three corrections to this handover: `docs/v2_lift/OPUS0_NOTES.md` — the confidence Kish copy was NOT verbatim identical to tabs (float vs rounded int, so the shared file offers both, named); maxdiff's divergence is a missing finite filter and no scale-safety, not rounding, so re-pointing is numerically inert and removes two NaN cases; and the specified `</` escape is superseded by escaping every `<`. Findings: `calculate_deff` is two different statistics sharing a name (NOT consolidated, needs a ruling); `report_hub/js/hub_pins.js` had the identical island bug outside the scoped inventory (hardened); `maxdiff/lib/html_simulator/js/simulator_pins.js` is a dead second pin engine, not a wrapper. No wrapper deleted. All eleven module suites green. **Fable pre-merge review owed.** |
| FAB-0 | Phase-0 part 1 — verify the 2026-07-02 audit stats bugs are closed in the live v2 JS. | Fable | **DONE 2026-07-10**: configurable alpha + Bonferroni divisor live (`21_stats.js:46-75,483-487`), weighted CIs use `baseEff` via `fpcBase` (`22_model.js:83`), FPC/effective-base machinery in `21c_confidence.js:184-188`. Checked directly in the JS, not from docs. |
| PROG-1 | cmdstanr + CmdStan toolchain into renv (needed for real Stan HB in **maxdiff** once maxdiff C1 is fixed; installation is an environment decision). Correction 2026-07-11: conjoint does NOT need this — its HB is bayesm (in renv.lock, installed), not Stan/ChoiceModelR. | Duncan | **DONE 2026-09-03**: cmdstanr + CmdStan 2.39.0 installed on Duncan's Mac (Xcode CLT toolchain), Bernoulli example sampled. `renv::snapshot()` is DONE: `renv.lock` carries a cmdstanr record at line 1660, checked 2026-09-16. First real Turas Stan fit the same evening found and fixed three extraction/lookup defects; Stan HB now recovers known utilities (rho 1.0) and the full pipeline runs on Stan. |

## 4. Module tracker

| Module | Review (Fable) | Handover | Implementation (Opus) | Pre-merge review | Merged |
|--------|---------------|----------|----------------------|------------------|--------|
| **maxdiff** | **DONE 2026-07-10** — `docs/v2_lift/MAXDIFF_PRODUCTION_REVIEW_2026-07-10.md`. 3 CRITICAL (silent EB-fallback mislabelled as HB; numeric resp_id phantom item in shares/TURF; ITEM_POSITION scores zero), template can't run, GUI success-on-refusal. | **DONE** — `docs/v2_lift/HANDOVER_MAXDIFF_FOR_OPUS.md`. Session A correctness / B report-fixes+tabs-exporter / C v2 (gated on OPUS-0, which is DONE and merged via conjoint). | **Session A implemented 2026-08-27** on `feature/maxdiff-correctness` (9 commits, one per work item A1 to A8 plus notes, unmerged, already on origin so no amending). **Review 2026-09-03 (Fable): FAILED as committed**, see `docs/v2_lift/REVIEW_FINDINGS_MAXDIFF_SESSION_A_2026-09-03.md`. The engine fixes for C1 to C3 and H1 to H6 verified in both directions, but the suite was NOT green at the tip (902 expectations pass, 4 tests ERROR, all test bugs), a refused HTML report still ended in TRS PASS, and C2 survived at seven report/simulator sites. **Follow-ups DONE the same day** as six new commits `7ddb716b`..`9f12a31e` (test corrections, F2 to F12, plus a pre-existing ITEM_SCORES loss of every utility column): suite now **945 / 0 fail / 0 skip / 0 error**; numeric-ID end-to-end run ships report, simulator and utilities. Old baseline observed 801/0/4. Stan path NOT sampled (cmdstanr absent, PROG-1). Notes: `docs/v2_lift/SESSION_A_NOTES_MAXDIFF.md`. B: NOT STARTED; C: NOT STARTED (unblocked; follow the conjoint island precedent, not §4 row kinds). **B2 + C DONE 2026-09-02** on `feature/maxdiff-v2-report` (branched off Session A's tip 6d3e0165; notes `SESSION_BC_NOTES_MAXDIFF.md`): tabs share export with the D5 gate, `{output}_md_island.json` plus a MaxDiff tab in tabs v2 (conjoint precedent, zero new row kinds), a shipped example (`examples/maxdiff`), the integrated demo (`examples/integrated_demo`). Found and fixed on the way: Session A's suite was not green at its tip (4 test bugs, the same fix as `7ddb716b`); the guard was not loaded when `00_main.R` was sourced from elsewhere; the standalone simulator paired utilities with items by position, its segment filter matched nobody, and it called the EB fallback "Hierarchical Bayes". Session B's report-fix half deliberately not done. The classic report was retirement-bound per conjoint C until **Duncan reversed that on 2026-09-03**: it goes to the tabs v2 standard instead, by widening the island and the tab to parity first and retiring only after he has proved parity on Karoo. Four steps in `HANDOVER_MAXDIFF_V2_FOLLOWUPS_FOR_OPUS.md` section 6. NOT STARTED at 2026-09-16: `27y_maxdiff.js` is still 281 lines against the classic report's seven panels, and `modules/maxdiff/lib/html_report/` is still in place. | **Session A MERGED to main 2026-09-03** (`db607112`, pushed). `feature/maxdiff-v2-report` brought up onto that main 2026-09-03 (see `HANDOVER_MAXDIFF_V2_REBASE_FOR_OPUS.md`). **v2-delta review DONE 2026-09-03: BLOCK for four fixes in the delta plus a pre-existing no-workbook hole** (`REVIEW_FINDINGS_MAXDIFF_V2_2026-09-03.md`); no wrong numbers anywhere. F1 to F5 fixed the same day by the reviewing session, main fast-forwarded to `0d964827` and PUSHED after Duncan's `launch_turas()` eyeball of Karoo, the demo and tabs. **MaxDiff is therefore MERGED and reviewed.** What is left is the follow-up handover `HANDOVER_MAXDIFF_V2_FOLLOWUPS_FOR_OPUS.md` (3 rulings, 7 items), which at 2026-09-16 has NOT been run: no divergence or ESS stamping in `12_tabs_export.R` or `13_v2_island.R` (R1), no `referenceItem` in the island or `27y_maxdiff.js` (R2), the guard resolvers still walk `seq_len(sys.nframe())` outermost-first in `conjoint/R/00_main.R:151`, `maxdiff/R/00_main.R:55` and `:141` and `tabs/run_tabs.R:20` (items 4 and 5), and `run_maxdiff_gui.R` still hard-codes the stats-pack checkbox to `value = FALSE` (item 9). Item 10, the gitignored Reader template, IS done. | — |
| conjoint | **DONE 2026-07-11** — `docs/v2_lift/CONJOINT_PRODUCTION_REVIEW_2026-07-11.md`. 3 CRITICAL (LC runs ship all-zero utilities labelled PASS; BW "simultaneous" attenuated by construction; HB/LC SE/CI/p/stars computed from heterogeneity SD). NOTE: HB is **bayesm**, not ChoiceModelR — no silent-fallback analogue (bayesm failure refuses loudly); no Stan dependency. Template's own dropdowns refuse runs (best_worst, greedy); GUI success-on-refusal; suite green only via structural skips (HB/LC/end-to-end untested). Tabs payload: per-respondent importance shares → Allocation (cleaner than maxdiff — no fallback, no weighting entanglement). | **DONE** — `docs/v2_lift/HANDOVER_CONJOINT_FOR_OPUS.md`. Session A correctness / B report-fixes+tabs-exporter / C v2 (gated on OPUS-0). | **Session A DONE 2026-08-27** on `feature/conjoint-correctness` (one commit per work item, unmerged — Duncan merges after regen). A1-A14 all landed; suite went from 0 fail / 12 skip to **0 fail / 0 skip**, with ~150 new assertions across 10 new test files. Notes and deviations: `docs/v2_lift/SESSION_A_NOTES_CONJOINT.md` — includes 9 findings the review did not have (every aggregate-path CI was NA; C3 was live on the latent-class path too; BW sequential recursed; two TRS refusals crashed instead of refusing). One decision-7 item needs Duncan's ruling: three functions it says to delete do have tests. **B2 + C DONE 2026-08-27** on `feature/conjoint-v2-report` (branched off Session A with OPUS-0 merged in; notes `SESSION_C_NOTES_CONJOINT.md`): per-respondent importance exported for tabs, `_cj_island.json` plus a Conjoint tab in tabs v2, the module's own HTML report DELETED, and the market simulator extracted standalone into `lib/html_simulator/`. | **All three reviews DONE 2026-08-27 (Fable), in dependency order.** Session A PASSED (HB and LC SEs hand-recomputed from `nmix$probdraw`/`compdraw`, matching exactly; the aggregate-CI-all-NA defect confirmed and fixed; suite 1079/0/0). OPUS-0 PASSED, clean to merge (old-vs-shared Kish 22/22 identical, min-base gate 147/147 identical, 800 weighted-test comparisons identical; one MEDIUM on silent sourcing skips awaiting a ruling). C-delta PASSED WITH 2 FINDINGS, both in the new surface: the extracted simulator's mode and export buttons called functions left behind in the deleted report JS, and a NULL `fit_statistics` serialised to a truthy `{}` so HB and LC runs showed a Model fit panel of dashes. **Both fixed and merged the same night.** | **MERGED 2026-08-27** (`f03ee8df`), since PUSHED; main is level with origin at `9df7b044` on 2026-09-16. Conjoint is COMPLETE. Two loose ends, neither blocking: the standalone simulator still escapes with `gsub("<", "\\\\u003c", ..., fixed = TRUE)` at `lib/html_simulator/99_simulator_main.R:114`, which emits a double backslash so a `<` in a segment name renders as the literal text `\u003c` (pricing dropped the `fixed = TRUE` and is correct); and `calculate_deff` still means `1 + CV^2` in confidence and `n / n_eff` in maxdiff, one name for two statistics, awaiting Duncan's ruling. |
| pricing | **DONE 2026-07-11** — `docs/v2_lift/PRICING_PRODUCTION_REVIEW_2026-07-11.md`. 3 CRITICAL (weighted studies get UNWEIGHTED VW price points under a "Weighting applied: Yes" stamp; stop-early GG designs silently inflate demand/optimal price via per-rung NA drops; VW CI table brackets a different estimator+population than the headline points). Template can't run VW or GG (Title_Case names, no loader map); 1/2-coded Yes/No → 100% intent; report Save loses insights + added slides. Extra-foci answers: VW intersections delegated to pricesensitivitymeter (sound); GG revenue math correct but smoothing is raise-only cummax (real PAVA is dead code); simulator JS math matches R **and is embedded, not standalone** (plan's D2 premise false for pricing — fork deleted, stays embedded); respondent-level tabs fit = VW 4 questions + monadic cell/intent are already tabs-native survey columns (QuestionMap only), GG acceptance grid → Multi_Mention 0/1 export, WTP is dead code (wire narrowly, D5-stamped). Curated call: **NO v2 view for pricing** (price-axis vs banner-axis mismatch); suite 0-fail but structurally green-biased (skip_if(!exists), no golden values, weighted paths untested). | **DONE** — `docs/v2_lift/HANDOVER_PRICING_FOR_OPUS.md`. Session A correctness / B report-fixes+tabs-exporter; no Session C. | **HANDOVER REFRESHED 2026-09-03**: `docs/v2_lift/HANDOVER_PRICING_V2_FOR_OPUS.md` supersedes the July brief (v2 island + Pricing tab on the maxdiff template, standalone simulator, classic report retired, Karoo pricing example first). **Session A BUILT 2026-09-03** on `feature/pricing-v2-lift` (Fable, same day as the refresh; notes `SESSION_A_NOTES_PRICING.md`): Karoo example + headless entry point, C1 to C3, H1 to H8, M1 to M14 as scoped, three new test files; suite 997 pass / 0 fail / 0 skip / 0 error / 14 warnings (63 before). **MERGED local main `299355dc` 2026-09-03** (Duncan's call, ahead of the review, to eyeball the Karoo example from the launcher), UNPUSHED; Duncan ran the Karoo config from `launch_turas()` the same evening and it completed. **Session B BUILT 2026-09-04** on `feature/pricing-v2-report` (notes `SESSION_B_NOTES_PRICING.md`): the v2 island `14_v2_island.R`, `pricing_island` in tabs, `27z_pricing.js` plus its node gate, the tabs export `15_tabs_export.R`, the standalone simulator, the classic report withdrawn (announce and continue, not refuse, because refusing every config in the field over a moved deliverable is wrong), and the integrated demo gaining pricing. Suites from their own runs: pricing 976/0/0, tabs 5,413/0/1, all 41 node gate files. Demo VERIFIED end to end after an Options-key fix: the rendered Pricing tab is 16,303 characters with no NaN and no dollar sign, and the GGACC crosstab renders all five rungs with banner breaks. Two tabs contract mismatches the July brief got wrong are now pinned by tests: a `Multi_Mention` cell must hold the option's own label, not a 0/1 flag, and its option rows are keyed BY COLUMN (`GGACC_1`), not by the question code. | **Fable review DONE 2026-09-03: PASS with fixes** (`REVIEW_FINDINGS_PRICING_SESSION_A_2026-09-03.md`). C1 to C3 verified in both directions with hand checks; suite 997/0/0/0/14 reproduced; 38 of 39 new test blocks fail on the old engine. Three items gate client use: long-format GG still reads 1/2 data as 100% intent via the new refusal's own advice (F1); the Results workbook Summary sheet prints the valid-weight count as Effective Sample Size (F2, fixed on `fix/pricing-review-a-followups` at `e8315e66`, unmerged, unpushed); the NMS extension fails with the template's PI_Scale and its weighted path is unverified (F3). Calibration: the 2% rung-base tolerance refuses full-presentation ladders at 2 to 3% item non-response (F4). Follow-up list in section 6 of the findings. **FOLLOW-UPS BUILT 2026-09-16** on `fix/pricing-review-a-followups-2`, eight commits off main `9df7b044`, UNMERGED: F1, F3, F4, F5, F6 and F7 to F12, on Duncan's 2026-09-03 rulings for F4/F6 and F3 (F2 was already in as `7fbfa461`). Suite 379 tests / 1,078 passing / 0 failed / 0 errors / 0 skipped from the repo root, against a session baseline of 348 / 984; pricing view gate 16, island encoding gate 18. The F4 proof Duncan asked for: the review's simulation table reproduces at 0 refusals in all twelve cells, and the Karoo stop-early config still refuses. Notes and the open items: `docs/v2_lift/NOTES_PRICING_REVIEW_A_FOLLOWUPS.md`. Still open by decision: F11's shared stats-pack em dashes, F13 and F14 (INFO), and whether an NMS figure ever reached a client. **Session B has NOT been independently reviewed**: it merged on 2026-09-04 without one, and there is no `REVIEW_FINDINGS_PRICING_SESSION_B*.md`. That is the only outstanding review in the whole programme. **Session B review DONE 2026-09-17: BLOCK on the tabs export and on Present mode, PASS WITH FIXES on the island, the tab and the withdrawal** (`REVIEW_FINDINGS_PRICING_SESSION_B_2026-09-17.md`). Suites reproduced from the repo root: pricing 379 tests / 1,092 expectations / 0 failed; tabs 1,741 / 5,945 / 0 failed / 1 skipped; 45 node gate files, 1,157 passed. Every island figure hand-recomputed and the tab rendered clean against real islands; the withdrawal broke nothing; both authored contract facts hold. F1 (CRITICAL, blocks the tabs export): tabs' Multi_Mention base is any-mention, so a respondent who accepts no rung drops out of the crosstab base and every rung share reads HIGHER than the module's, while the METHOD sheet says it reads lower and says filtering on pricing_valid restores the base, which it cannot; on the shipped stop-early-imputed config the cheapest rung reads 100.00% in tabs against 93.77% published, by construction (25 of 400 all-blank rows, base 375); Karoo both-methods 94.4% vs 93.8%; the shipped demo 520 in base of 524 valid. F15 (HIGH): a long-format study cannot use the export and is told to fix its ids. F16 to F20: a refused island/export/simulator still ends at TRS PASS, the snippet names a QuestionMap sheet tabs does not read for that, an empty currency dies on the CSV path, 10+ rungs sort as text, and two tests pin the false METHOD wording. F2 (HIGH, blocks every tabs report): the Pricing tab's `.pr-note`, `.pr-table`, `.pr-chart` classes collide with Present mode's in `styles.css`, restyling Present mode in every report on main. F11 (HIGH): a VW-only run with the simulator on writes an island naming a simulator file step 9 then refuses to write, so the tab links to nothing. F12 (HIGH, carried across from the old report): Simulator-sheet scenarios never reach the page (loader stores `simulator_scenarios`, page reads `config$simulator$scenarios`, column `Product_Price` versus `price`). Simulator math parity, P1c/P2a/P3a and minification verified by the simulator reader, not re-run by the coordinator; the withdrawal-and-demo reader stopped without findings (permanent hole beyond the coordinator's own withdrawal runs); the author's notes were not read. | **Sessions A and B both MERGED and PUSHED**; main level with origin at `9df7b044` on 2026-09-16. |
| keydriver | **DONE 2026-07-11** — `docs/v2_lift/KEYDRIVER_PRODUCTION_REVIEW_2026-07-11.md`. 2 CRITICAL ("Johnson's Relative Weights" math is wrong — PCA orthogonalisation instead of symmetric square root; 2 drivers → always 50/50 regardless of data, wrong in all 3 paths incl. bootstrap CIs, coordinator-verified numerically; segment-comparison layer wrong 4 ways — unweighted under weighted studies, categorical drivers fabricated 0%, Segments-sheet groupings ignored, failures incl. refusals swallowed under PASS). Extra-foci answers: **Shapley/LMG is correct** (exact all-subsets, weighted refits — headline column trustworthy); VIF formula correct (unweighted aux, minor); correlation-vs-regression labelling honest in core table. 12 HIGHs: shipped template cannot load (empirically proven — headers row 5/3 vs loader startRow=1; fix loader-side, tracker has the pattern); GUI success-on-refusal; Run_Status stamps dead-code "partial_r2" as primary method (whole v10.3 engine unwired); mixed-path RW silent r²-proxy fallback; weighted dominance ≠ WLS (verified empirically); SHAP dummy-collapse regex can never match (verified); NCA likely inert (SUSPECTED); 14-check preflight suite dead code; Insights/CustomSlides double-dropped; Save loses slide commentary (textarea class); quadrant source dropdown values silently ignored. Suite 0-fail/928-pass but structurally green (no template round-trip, RW tested only sum=100, 2 dead skips, 574 lines of tests for never-called preflight). Tabs integration: **ruled out definitively** (aggregate-only, no per-respondent artefacts). V2: plan's "`index_scores` row pattern" premise FALSE (it's a microdata mean-recompute map; keydriver has no microdata) — route = tracking-style frozen `TR.KD` island + own workspace view, ZERO new row kinds; supersedes V2_MIGRATION_PLAN :41/:55. | **DONE** — `docs/v2_lift/HANDOVER_KEYDRIVER_FOR_OPUS.md`. Session A correctness / B config+template+GUI+report / C v2 island+view (gated OPUS-0). | **Session A implemented 2026-09-17** on `feature/keydriver-correctness` (16 commits, one per work item A1 to A11 plus the example, the notes and the shared loader fix; unmerged, unpushed; rebuilt from `6d0995db`, 12 commits behind main, merges clean). B: `feature/keydriver-config-and-report-fixes`; C: `feature/keydriver-v2-report`, both not started. | **A reviewed 2026-09-17** (Fable, `REVIEW_FINDINGS_KEYDRIVER_SESSION_A_2026-09-17.md`, uncommitted). Verdict PASS WITH FIXES: 0 CRITICAL, 7 HIGH, 12 MEDIUM, 10 LOW. C1, C2, H3, H4, H5 closed and re-derived by execution in both directions; H6 (SHAP on a factor driver) still fails in production; pre-flight is wired only when the caller sources it and two checks refuse or degrade clean configs; M6 half fixed; `random_seed` dead for the bootstrap; four disclosures written nowhere. Suites from the repo root: keydriver 395 tests / 1,202 expectations / 0 failed / 2 skipped; shared 465 / 1,348 / 0 / 4; catdriver 227 / 676 / 0 / 1. | not merged | **Review follow-ups DONE 2026-09-18** on the same branch, four commits `3165cd73`..`945b4494`: all 7 HIGHs, 9 MEDIUMs and 4 LOWs. Keydriver 414 / 1,389 / 0, shared 465 / 1,325 / 0, catdriver 227 / 673 / 0. F26 (what quadrant "auto" should prefer) left as Duncan's call. **Session B DONE 2026-09-18** on `feature/keydriver-config-and-report-fixes` in worktree `~/Dev/Turas-keydriver-b`, seven commits, UNMERGED: GUI verdict, one bootstrap default, commentary that survives Save Report, the report config plumbing (the loader read six sheets and the template shipped four), eight report-layer mediums, the Settings whitelist, and the doc drift. Suite 426 / 1,506 / 0. **Merge the correctness branch FIRST**: Session B was cut before the review follow-ups and both touch `generate_config_templates.R` and the bootstrap call site. Session C (v2 island + view) NOT STARTED, unblocked. **Session C DONE 2026-09-18** on `feature/keydriver-v2-report` in worktree `~/Dev/Turas-keydriver-c`, one commit `f992857f`, UNMERGED: the `TR.KD` island (`modules/keydriver/R/13_v2_island.R`) and the Key drivers tab (`27k_keydriver.js`), wired through `keydriver_island` in the tabs config. Frozen tab, zero new row kinds. Shapley stamped "no interval available"; the CI level travels in the island so a 90% study cannot be labelled 95%. Segment engine now keeps each segment's base; nEff from the shared Kish. Keydriver 460/1,775/0, tabs 1,804/6,193/0, all 47 node gates pass. Verified by opening a built report, which found the clipped quadrant label and the raw variable names. **Keydriver is now complete through all three phases.**
| catdriver | **DONE 2026-07-11** — `docs/v2_lift/CATDRIVER_PRODUCTION_REVIEW_2026-07-11.md`. 3 CRITICAL (weighted multinomial importance mathematically invalid — weighted full vs unweighted reduced LR → negative chi-squares, garbage ranking under PASS, coordinator-verified numerically; subgroup-comparison feature dead on arrival — consumer reads `driver/level/or` columns the mapper never produces, errors on every real run, swallowed to silent NULL under PASS, coordinator repro'd; refusal system broken module-wide — stale `00_guard.R` `catdriver_refuse` signature clobbers the live one in GUI source order, every refusal presents as BUG_INTERNAL_ERROR with fix text destroyed, coordinator repro'd — and the test helper sources in the *opposite* order, so the suite binds the correct signature production never sees). Extra-foci answers: **SHAP focus is void** — no SHAP/shapr code exists anywhere in the module; claim lives only in stale platform docs (launch_turas.R:153, CLAUDE.md:57, README.md:30); dummy coding/term mapping is the *strongest* part (09_mapper.R model-matrix introspection + hard validate gate); sample-size gates wired but EPP gate computes observations-not-events per parameter (~10× optimistic). 11 HIGHs incl.: weighted McFadden R² negative (unweighted null), weights never normalised (SEs scale with raw weight sum; Kish deff computed but display-only), ordinal direction-sanity guard discards its warning AND arity-crashes when it fires, no-Order ordinal outcome silently alphabetical, bootstrap silently vanishes on weighted binary, multinomial_mode facade (4 modes demanded, only baseline_category exists), template can't load (headers row 5/3 vs loader row 1 — fix loader-side), GUI success-on-refusal feeds failed runs to unified report, documented CLI entry broken, stats pack provenance wrong 4 ways. Report layer: unified Overview tab renders BLANK (flagship deliverable, browser-verified); Save loses slides (textarea .value); shared `turas_pins.js` JSON island not `</script>`-hardened (ALL TurasPins modules affected); ORs narrated as "x more likely". Suite 0/4/1/659 but structurally green (subgroup tests never build odds_ratios; source-order masks the refusal clash). Tabs integration: **ruled out** (aggregate outputs; fitted probs D5-inadmissible + redundant) — second no-exporter module. V2 landing zone (module absent from V2_MIGRATION_PLAN scope): frozen `TR.CD` island + own view, zero new row kinds, sequenced AFTER TR.KD on its scaffolding; S-M effort. | **DONE** — `docs/v2_lift/HANDOVER_CATDRIVER_FOR_OPUS.md`. Session A correctness+refusal-system / B config+template+GUI+report (incl. shared turas_pins hardening) / C v2 island+view (gated OPUS-0 + TR.KD). | **Session A DONE 2026-09-18** on `feature/catdriver-correctness` (10 commits, one per work item plus the notes; UNMERGED, UNPUSHED; cut from main `46eb9a01`). All three CRITICALs and H1-H6, H11, M1-M6, M11's engine half fixed with tests that were watched failing on the old code: the refusal system (every refusal was BUG_INTERNAL_ERROR), weighted multinomial importance (chi-squares of -250 and -259 on the branch fixture, so every driver showed 0 per cent and p = 1), the dead subgroup comparison (now four groups, nine driver-levels, proven on the module's own demo end to end), weights normalised with an inference stamp, the ordinal direction guard (it crashed the run the moment it fired), an ordinal outcome with no Order (refused), the bootstrap (it had never once produced an interval for a weighted binary study), multinomial_mode amputated to baseline_category, the stats pack's four wrong stamps, the EPP gate (observations, not events), the proportional-odds assumption (never tested on the default engine), and probability lift relabelled for what it is. ~3,000 lines of dead code deleted with CODE_INVENTORY corrected. Notes: `docs/v2_lift/SESSION_A_NOTES_CATDRIVER.md`. Suite 278 tests / 919 passing / 0 failed / 1 skip, from 227 / 673; shared 469 / 1,330 / 0. The suite had NO test that ran the pipeline: there is one now. B: `feature/catdriver-config-and-report-fixes`; C: `feature/catdriver-v2-report`, neither started. | **A reviewed 2026-09-19** (Fable, independent, `REVIEW_FINDINGS_CATDRIVER_SESSION_A_2026-09-19.md`, committed on the branch). Verdict PASS WITH FIXES for the merge, BLOCK on two paths: 2 CRITICAL, 3 HIGH, 16 MEDIUM, 9 LOW, 3 observations. C1, C2, C3, H1, H2 and H5 closed and re-derived by execution in both directions against implementations written from the definitions (weighted multinomial LR shares agree to 4e-4 where main shipped chi-squares of -212 to -242 and 0 per cent for every driver; SE ratio 1.0000 under a fivefold weight rescale where main gave 0.4472; a broken config refuses with its code and fix text where main said BUG_INTERNAL_ERROR). Unweighted binary and ordinal runs are numerically identical to main in every table. The CRITICALs are wrong numbers on paths July did not reach and block those paths, not the merge: a multinomial subgroup comparison overwrites outcome levels into one unlabelled row (F3, coordinator re-run: Premium OR 11.36 and Standard OR 2.18 become one row reading 2.18); the ordinal probability lift is each respondent's probability of their own observed category, main identical, now labelled and explained as a lift (F4; run ordinal reports with probability_lifts FALSE until fixed). The HIGHs are provenance: the ordinal importance stamp says likelihood-ratio and car::Anova on a clm is Wald (F1); a misspelled, character or all-NA weight column runs unweighted under PASS with a Declaration that says weighted (F2); the Declaration's two respondent counts are swapped (F5). Most MEDIUMs are one pattern: the right words written into channels no sheet reads (guard warnings, proportional_odds, method, outcome_level, the stats-pack event problem). Test gaps: H1 and M1 have no discriminating test and C1's headline test passes on the reverted code. Suites from the repo root: catdriver 278 tests / 921 expectations / 0 failed / 1 skipped; shared 469 / 1,353 / 0 / 4; main baseline 227 / 676 / 0 / 1. Not verified: any GUI run, the unified report, a natural car::Anova failure. | **ALL THREE SESSIONS MERGED AND PUSHED 2026-09-19**, origin/main `5135500f`. Catdriver is COMPLETE. Owed: Duncan's `launch_turas()` eyeball; TR.CD has never been through `turas_minify`; the classic report is deliberately NOT retired. Sessions B and C were NOT independently reviewed (only A was). | **ALL 30 REVIEW FINDINGS FIXED 2026-09-19**, `87db7f82` (F1 to F5) and `b3a89f24` (F6 to F30), each with a test. Two further defects found while verifying, neither filed by the review: a positional column rename in the Subgroup Model Fit sheet gave a column the name NA the moment the frame gained one, so openxlsx wrote every subgroup workbook that day with an EMPTY shared-string table (Excel would offer to repair it, reading it back segfaulted R); and a PARTIAL carrying no affected output is refused by the shared run state, which killed an ordinary multinomial run outright once the events-per-parameter fix made a stability flag reachable. Both now tested. The ordinal run also loses a degraded reason it never deserved: car::vif on a clm was reporting GVIF in the tens of thousands, so collinearity is measured on the design matrix instead. Suite 298 tests / 1,018 passing / 0 failed / 1 skip, from 227 / 673 at session start; shared 469 / 1,330 / 0; all six demo configs write workbooks that read back. Session A MERGED and PUSHED 2026-09-19 (origin/main `08d92c90`) after Duncan's eyeball. **Session B DONE 2026-09-19** on `feature/catdriver-config-and-report-fixes`, four commits, UNMERGED: the GUI now treats a refusal as a failure (it tested for "REFUSED" while the handler returns "REFUSE", so refused configs were announced as complete and fed to the unified report); the unified report's Overview renders (verified headless, before and after); slide commentary survives Save Report; subgroup chart labels are escaped; odds ratios are narrated as odds; one loader replaces three copies of the source order and the documented entry point works for the first time; the template no longer offers values the guards refuse; the stats-pack checkbox, the unknown-setting silence and the GUI branding override are all fixed; the SHAP claim is gone from the platform docs; and the dead JS pair plus the dead comparison generator are deleted now that the startup guard checks the files the reports actually embed. Notes: `docs/v2_lift/SESSION_B_NOTES_CATDRIVER.md`. Suite 319 / 1,140 / 0 failed / 1 skip, from 298 / 1,018. **Session C DONE 2026-09-19** on `feature/catdriver-v2-report`, UNMERGED: the frozen `TR.CD` island (`modules/catdriver/R/13_v2_island.R`) and the Categorical drivers tab (`27j_catdriver.js`), wired through a new `catdriver_island` setting on the tabs side. Zero new row kinds. The view says odds and never likelihood, carries no significance letter, draws a bootstrap whisker only where a bootstrap ran, and labels the interval at the level the study used. The audience strip stopped printing the crosstab project's base above the frozen contribution tabs, which affected conjoint, maxdiff, pricing and keydriver too. Verified by building a v2 report on the committed parity data layer with an island from a real demo run and rendering it headless. Catdriver 326 / 1,189 / 0; tabs R 1,808 / 6,213 / 0; all 48 node gate suites pass, one of them new with 16 gates. Drop-list logged in V2_MIGRATION_PLAN section 7; island contract documented beside TR.KD in 11_DATA_CENTRIC_REPORT_V2.md. Notes: `docs/v2_lift/SESSION_C_NOTES_CATDRIVER.md`. The classic report path is NOT retired: that needs Duncan's say-so. **catdriver is now complete through all three sessions.** |
| segment | **DONE 2026-07-11** — `docs/v2_lift/SEGMENT_PRODUCTION_REVIEW_2026-07-11.md`. **Best-shape module of the six.** 2 CRITICAL (mini-batch k-means dead for any n>10,000 — dispatcher passes an `nstart` arg the function doesn't have → raw crash in final mode / MODEL_ALL_K_FAILED in exploration, coordinator-verified by execution; LCA is an advertised, config-exposed silent no-op — README instructs `use_lca=TRUE`, config parses it, nothing reads it, 767-line 11_lca.R unreachable, found independently by two readers). 3 HIGH: GUI success-on-refusal (sixth module of six); scoring/typing non-kmeans models silently applies the k-means nearest-centroid rule (GMM assignments don't reproduce); `validate_segment_config` silently drops unenumerated template keys (stats-pack Y/N + research_house are dead controls). Extra-foci answers: **assignment-file bridge does NOT exist** — `Segment_Assignments` is written as "the join table for the segment-as-banner workflow" but repo-wide grep finds zero consumers; the bridge is Duncan's manual merge. Parked `feature/segment-report-v2` reconciled: stays parked (its 9k-line vendored engine is a stale copy of tabs-v2); its own PLAN.md division-of-labour (lean HTML + crosstabs via tabs banner) independently matches the review's route; salvage list documented. All 6 fixes of the 2026-06 v1 bug audit verified present on main. Good news: template loads (row-1 headers — keydriver/catdriver defect absent), preflight validators actually called, save-a-copy safe (contentEditable), escaping sound, live kmeans/hclust/GMM/validation math verified defensible, TRS strong. Integration: **cleanest fit in the programme** — assignment IS tabs-native microdata → tabs exporter (effort S), segment-as-banner recomputes profiles LIVE in v2, **no v2 view, zero new row kinds** (supersedes V2_MIGRATION_PLAN :41/:55 for segment — 5th/6th false plan premises); diagnostics stay classic under D1; D5 stamps: model-derived grouping, in-sample-sig caveat on clustering vars, GMM probs refuse-as-weights; unweighted-clustering vs weighted-tabs mismatch documented. Suite 0/1/1/1026 — most honest of the six, but its one skip green-skips a real validate_input_data bug, and dead features carry live tests. | **DONE** — `docs/v2_lift/HANDOVER_SEGMENT_FOR_OPUS.md`. Session A correctness (LCA+ensemble amputated) / B GUI+report hygiene / C tabs exporter (NOT gated on OPUS-0). | **Session A DONE 2026-09-20** on `feature/segment-correctness`, seven commits `d3ba63d6`..`fb4f7bc5`, **MERGED and PUSHED 2026-09-20**, origin/main `226bb952`. C1, C2, H2, H3, M1, M2, M3, M4, M7, M8 all fixed with tests watched failing first. Mini-batch was dead for every study over 10,000 rows; LCA and ensemble amputated and a config that still asks for either is refused by name; scoring a GMM or hclust model by the k-means rule now refuses; two dead template controls carried through plus a console warning naming any setting that does not survive validation. Suite 1,002 passing, **0 fail / 0 warn / 0 skip** from a 1,026 / 1 / 1 baseline, which is the zero-skip acceptance signal. The doc scrub was in ten files, not the two the handover named. Notes: `docs/v2_lift/SESSION_A_NOTES_SEGMENT.md`. **Session B DONE 2026-09-20** on `feature/segment-gui-and-report-fixes`, five commits `461cf490`..`ad99b3f7`, **MERGED and PUSHED 2026-09-21**, origin/main `4cdfc0bf`. H1, M5 and all five LOWs. The GUI called every refusal a success; the k-selection table was dropping the silhouette column it recommends on, over a name mismatch; the combined report's comparison table was THROWING on every run and even its warning was lost to `<-` in a closure; the Variable Importance section asserted eta-squared while normalising F statistics. Suite 1,112 passing, 0/0/0. Notes: `docs/v2_lift/SESSION_B_NOTES_SEGMENT.md`. **Session C DONE 2026-09-21** on `feature/segment-tabs-export`, **MERGED and PUSHED**, origin/main `421572bc`: the tabs banner bridge. `tabs_export = Y` writes the survey file with a `segment_name` column plus a banner stub whose rows paste straight into Survey_Structure and the tabs config. The join refuses duplicates and refuses a partial match unless `allow_partial_join = Y`, because unmatched rows would otherwise look like a finding about people. GMM probabilities never travel (D5). The stub is tested against tabs' OWN `create_banner_structure()`, which caught a missing `BannerBoxCategory` column that would have killed the banner build. Segment 1,185 / 0 / 0 / 0; tabs 6,215 unchanged. Notes: `docs/v2_lift/SESSION_C_NOTES_SEGMENT.md`. **Segment is now complete through all three sessions.** | **Review DONE 2026-09-21** (Fable, `REVIEW_FINDINGS_SEGMENT_2026-09-21.md`, on branch `review/segment-v2-lift`, not merged). Verdict MERGE AFTER FIXES, no revert: both CRITICALs and all three HIGHs closed by execution both ways; Shiny server executed for the first time in the programme; exported file run through tabs itself. Reviewer fixed three things on the branch (a PARTIAL run announced as a clean success in the GUI; four fixes with no test that noticed a revert; the doc scrub residue) and left Duncan five rulings: the tabs export refuses any study with missing data or removed outliers after the assignments file and before every other deliverable; the stats pack is checkbox-only in the GUI; demographic profiling and `k_selection_metrics` are pre-existing silent no-ops; combined mode ignores `tabs_export`. Suite on the branch 1,213 / 0 / 0 / 0. **Rulings 1, 2 and 4 given and applied the same day** (`ebc04f93`): module-dropped rows are Unassigned with a PARTIAL run rather than a refusal; the checkbox starts as the config's setting; CH and DB are computed on every run and shown everywhere, `k_selection_metrics` selects them and refuses unknown names. Suite 1,258 / 0 / 0 / 0. **Session D DONE 2026-09-21** (Opus, `feature/segment-demographics` off the review branch, four commits, not merged): F14 refused at validation with `CFG_TABS_EXPORT_COMBINED`; F2 commissioned, so a run that names `demographic_vars` profiles them into a Demographics section and four workbook sheets, with a refusal for a name the data does not carry and a console note in combined mode; plus the three LOW wording items F8, F9 and F17, and two gaps an adversarial pass found afterwards. Suite 1,339 / 0 / 0 / 0; the Thornhill example run to a scratch folder shows all three demographics. Notes `SESSION_D_NOTES_SEGMENT.md`; its independent review `REVIEW_BRIEF_SEGMENT_D.md` is NOT started. | Duncan: run the Session D review, regenerate through `launch_turas()`, merge both branches. |

| **brand** (late addition 2026-07-12) | **DONE 2026-07-12** — `docs/v2_lift/BRAND_PRODUCTION_REVIEW_2026-07-12.md`. Second-best-shape module after segment. 2 CRITICAL (Dirichlet/Double-Jeopardy *expected* values ALL NA under PASS — `get_field` probes fields the NBDdirichlet object doesn't have, coordinator-verified by execution; the element's entire observed-vs-norm purpose has never worked + latent single-brand crash. Report "Save" silently loses analyst insight commentary — three divergent persistence mechanisms (sessionStorage/localStorage/unsynced textareas), none survives `outerHTML` serialization, coordinator-verified). 5 HIGH: portfolio category penetration UNWEIGHTED beside weighted cells (coordinator-verified 09h:302); ALL sig/CIs design-effect-naive — weighted estimates on raw Σw, no Kish n_eff, found independently by two readers (brand's primary OPUS-0 dependency); Audience-Lens localStorage key collides across clients; misattribution off-list credit drop (SUSPECTED, needs option-domain ruling); role-map build failure silently drops ALL v2 elements under GUI success. Good news (first of seven): GUI honours engine refusals, shipped templates load (both plagues absent, empirically checked), weights threaded module-wide, funnel math correct, loader whitelist clean. Penetration "five numbers" root-caused: 3 buyer definitions × weighted/unweighted × 2 screener windows — inventory in review §6 (closes reconciliation deferred item via labelling + callout). Suite 2155/0/2/1 but Dirichlet tests assert existence not values (green-bias on exactly C1). Integration: **FOURTH archetype — v2-standard upgrade IN PLACE** (brand is absent from V2_MIGRATION_PLAN entirely; its HTML report IS the deliverable): NO tabs exporter (non-duplication), NO island/view, adopt OPUS-0 plumbing (Kish into sig closures, shared disclosure gate, retire brand_pins.js) + native OOXML PPTX. Parked `review/brand-report-v2-upgrade-2026-06` reconciled: doc still-true, merge its docs-only commit (6c1f0e41), close branch; Rec A clarity-IA = separate Duncan-scoped design effort. Memory correction: `feature/branded-reach-and-dba` already merged to main. | **DONE** — `docs/v2_lift/HANDOVER_BRAND_FOR_OPUS.md`. Session A engine correctness / B report+GUI fixes / C OPUS-0 adoption + native PPTX (gated OPUS-0); no exporter, no v2 view; Rec A excluded. | **A + B + C DONE 2026-09-03** (Fable, one branch `feature/brand-v2-lift`, in a worktree; NOT merged, NOT pushed). Notes: `docs/v2_lift/SESSION_NOTES_BRAND_2026-09-03.md`. C3 (native OOXML PPTX) deferred to its own session. | Duncan: regenerate via launch_turas(), eyeball, merge | Fable pre-merge review owed |

| **weighting** (late addition 2026-07-12) | **DONE 2026-07-12** — `docs/v2_lift/WEIGHTING_PRODUCTION_REVIEW_2026-07-12.md`. Calculations-only scope (HTML report layer deliberately unreviewed — weights feed tabs). 2 CRITICAL (config post-hoc trimming breaks sum-to-n AND calibrated margins — `rescale_after_trimming` is dead code, no re-rake, diagnostics still report raked margins as achieved; NA weights flow into the tabs lookup unflagged — unmatched strata/cells, NA-as-string cell keys, and a failed weight in PARTIAL mode writes an all-NA column). 4 HIGH: design weights never normalised in main pipeline (mean = pop/sample, asymmetric with rim); empty target cell skipped w/ warning shrinking weighted base; no duplicate-respondent-ID guard on the merge-back lookup; `converged=TRUE` hardcoded, achieved margins never asserted vs tolerance. Core verified sound: raking = survey::calibrate (name-matched, refuses on non-convergence, sums to n), row alignment correct end-to-end, no rounding on write, Kish n_eff/DEFF in diagnostics.R exact and tabs-consistent. Suite green but numerically weak (almost no exact-value assertions). Integration: NONE — no exporter, no view, no v2 report work; deliverable is correct weights in the lookup file. | **DONE** — `docs/v2_lift/HANDOVER_WEIGHTING_FOR_OPUS.md`. Single session, W1-W8; 4 §0 decisions for Duncan (trimming semantics, design-weight scale, NA-row policy, PARTIAL output). | **W1-W8 DONE 2026-08-09**, MERGED to local main (unpushed) at Duncan's instruction before his launch_turas pass — he had no current weighting project to verify against, and main is local and revertible. Built on the §0 locked defaults, none vetoed. W1 rim post-hoc trimming refused (CFG_TRIM_USE_CAP), design/cell trim rescaled to original sum + disclosed; W2 convergence judged on achieved margins via judge_margin_convergence(), new `margin_tolerance` setting, PARTIAL when missed; W3 design/cell refuse DATA_UNWEIGHTED_ROWS with `allow_unmatched` opt-in, empty target cells refuse with the population share they cost; W4 failed weights omitted from the lookup file instead of written all-NA; W5 design weights normalised to sum = n, `grossing = Y` keeps population scale; W6 duplicate/missing IDs and weight-name collisions refused; W7 unreadable targets refused, labels trimmed both sides (case still respected), exported cores validate targets, DEFF from unrounded n_eff; W7c NA-before-key-construction + unit-separator cell keys; W8 numeric batch. Suite 268 tests / 679 expectations / 0 failed (was 209/454). New `docs/RUNBOOK.md` with a full refusal index. Not done, noted deliberately: rim-on-design still unreachable via config (feature decision, LOW), rim's refuse-on-NA unchanged (M7, separate decision). | — |

**Baseline-drift note (all sessions):** suite pass/fail/skip counts quoted in handovers are as-of their review date and indicative only — capture your OWN baseline at session start (run the suite before your first change) and measure "count must not drop" against that, not the quoted numbers.

**Sequencing note (shared pins island):** the shared `turas_pins.js` `</script>`-hardening fix is owned by OPUS-0 (W3 in `HANDOVER_OPUS0_FOR_OPUS.md`, pulled in from catdriver B5, which is marked superseded — a Duncan veto of OPUS-0 §0.1 returns it to B5); it must land before segment or brand reports are regenerated for release, since those handovers harden only their own R-side islands.

Already at v2: tabs, tracker. Out of scope: report_hub (post-programme per NEXT roadmap). Brand was originally out of scope ("own roadmap") — added to the programme 2026-07-12 with its review row above.

## 5. Log

- 2026-09-21: **segment Session D implemented** (Opus 5, `feature/segment-demographics` cut from `review/segment-v2-lift`, four commits, not merged, not pushed; notes `SESSION_D_NOTES_SEGMENT.md`). Duncan's two rulings from the 21 September review carried out: `tabs_export = Y` in combined mode is refused at validation rather than ignored, and demographic profiling was commissioned rather than removed. `profile_demographics()` gained its first caller in the module's history; a Demographics section sits after Profiles behind `html_show_demographics`, and the Excel report carries `Demographics_Tests` plus one `Demo_<variable>` sheet each. A demographic name the data file does not carry is refused at load, before clustering. Combined mode prints a skipped-section note rather than ignoring the setting. Eight deviations are logged in the notes; the substantive ones are that numeric demographics render without a p-value (the function does not return one), that the chi-square error branch was repaired (it wrote to a local copy and built a row of the wrong width), and that a header-alignment defect in a table that had never been rendered was fixed. Suite 1,339 / 0 / 0 / 0 against a 1,258 baseline; the whole new test file was watched failing against the pre-D2 source (13 failures). Next: the independent review in `REVIEW_BRIEF_SEGMENT_D.md`, then Duncan's `launch_turas()` eyeball and the merge.
- 2026-09-03: **brand Sessions A, B and C implemented** (Fable, `feature/brand-v2-lift` in a separate worktree, not merged, not pushed; notes `SESSION_NOTES_BRAND_2026-09-03.md`). Both CRITICALs closed with executed proof: the Dirichlet expected values now come from the NBDdirichlet object's closures (IPK fixture run end to end: finite values, flags no longer all on line) and analyst commentary survives Save through one DOM-serialisable store (headless Chrome typed into four box types, dumped the DOM, reloaded it, read the text back). All five HIGHs and every MEDIUM and LOW addressed; H2 rewired onto the shared Kish n_eff and M4 onto the shared disclosure gate (both OPUS-0 sockets, first consumer outside tabs). Two defects the review missed: the Branded Reach structure wrapper called itself with the engine's arguments, so the element silently refused on any project with a MarketingReach sheet; and the shipped MarketingReach template lacked the three question-code columns the engine requires. A `portfolio_extension_baseline = buyers` template value that the engine never implemented now refuses. M7 lands as on-face base labels on six surfaces plus a per-category "Which penetration is which" block on the Summary tab. Brand suite 2155 to 2311+ passing, 0 failures. Deferred with reasons: C3 native PPTX; the funnel sig closures stay dead in production (`sig_tester = NULL`), a product decision. Next: Duncan regenerates and eyeballs, then a Fable pre-merge review.
- 2026-09-02: **maxdiff Sessions B2 + C implemented, plus the examples** (Fable, `feature/maxdiff-v2-report` off Session A's tip, not merged, not pushed). The maxdiff twin of conjoint's arrangement: `12_tabs_export.R` writes per-respondent preference shares as a tabs Allocation question behind the D5 gate (EB refuses unless `Allow_Approx_Utilities_Export`, then stamped); `13_v2_island.R` writes `{output}_md_island.json`; tabs gains `maxdiff_island`, a `data-md` island, `27y_maxdiff.js` and a MaxDiff tab (READ group, frozen, filter bar hidden). Both modules now ship an example that runs headless from the repo root, and `examples/integrated_demo/build_integrated_demo.R` builds survey + conjoint + MaxDiff for one respondent frame and produces a single v2 report with both tabs, both Allocation crosstabs and both simulators. Five findings fixed on the branch, three of them in code nobody had executed: Session A's suite had 4 failing tests at its tip; the guard lookup used the caller's directory so headless refusals crashed; the standalone simulator paired utilities and items by position (wrong items everywhere), had a segment filter that matched nobody since Session A's config change, and called the EB fallback "Hierarchical Bayes". Notes: `SESSION_BC_NOTES_MAXDIFF.md`. Next action: Fable independent pre-merge reviews in dependency order (Session A, then this delta), then Duncan's `launch_turas()` eyeball on the two examples and the demo, then merge.
- 2026-09-03: **maxdiff Session A independent pre-merge review (Fable): FAILED.** `docs/v2_lift/REVIEW_FINDINGS_MAXDIFF_SESSION_A_2026-09-03.md`. Every CRITICAL and HIGH fix executed both ways (fails on the `f03ee8df` export, passes on `6d3e0165`) with a hand-recomputed number each: shares 65.861/27.525/6.615, position-decoded counts to the digit, a 60-respondent weighted end-to-end run matching hand-computed Best%/Worst% to 6 dp. Blocking: the branch fails its own definition of done. Four tests error at the tip while the notes, the A8 commit and this tracker said 902/0/0; the erroring test includes the one that proves C3. All four are test bugs (the corrected files from `feature/maxdiff-v2-report` pass 83/0 against this engine). HIGH: a refused HTML report leaves the run at TRS PASS (`00_main.R:1264-1274`, observed); C2 survives in seven report and simulator consumers (simulator utilities shift by one item under numeric IDs; the diagnostics transformer crashes and the report refuses). HIGH, pre-existing and missed in July: ITEM_SCORES never carries Logit or HB columns (double merge, `.x/.y`). Stan path still not sampled (PROG-1). **Same day, follow-ups landed** (six commits `7ddb716b`..`9f12a31e`, not pushed): the test corrections, F2 (late events fold into the run state), F3 (ID stripped at all seven report/simulator sites), F4 (ITEM_SCORES keeps its utility columns), F6 to F12, template regenerated with the stats pack on. Suite 945 / 0 / 0 / 0. The numeric-ID end-to-end run now ships every deliverable; a stubbed report refusal ends PARTIAL with the event named. F5 ruled by Duncan and implemented the same day (`9591c187`): one meaning for HB_Utility_SD on both paths, precision in a new HB_Mean_SE. Suite 957 / 0 / 0 / 0. **Later the same evening, PROG-1 closed**: Duncan installed cmdstanr + CmdStan; the Turas Stan model sampled on its first real run (C1 confirmed) and then crashed in extraction on a draws-array index, its divergence check had never run, and the model file was cwd-relative; all fixed, a gated Stan recovery test added (rho 1.0, anchor honoured, HB_Mean_SE filled, 0 divergences), and the synthetic pipeline completes on Stan. Suite 963 / 0 / 1 skip / 0. Next action: Duncan regenerates via launch_turas() and merges.
- 2026-09-03: **maxdiff Session A merged to main, and the v2 branch brought up onto it** (Opus, `feature/maxdiff-v2-report`, still unmerged and unpushed; notes `MERGE_NOTES_MAXDIFF_V2_2026-09-03.md`). Duncan merged `feature/maxdiff-correctness` and pushed (`db607112`). `git merge main` into the v2 branch touched 23 overlapping files, not the 15 the handover listed, because main had also moved on the tabs side; three conflicted and were resolved per the handover, and the config template was regenerated so the binary carries both sides. The merge then found two defects of the same kind, code written on one branch before a ruling landed on the other, invisible to git because neither side touched the other's file. **F2 again**: the B2 export and the C island run after `run_result` is built and used `add_warning()` alone, so an export refusal printed in full and the run still closed `[TRS PASS] COMPLETED SUCCESSFULLY` with an empty warnings vector; `note_late()` now carries each step's own refusal code. **F5 one layer out**: the MaxDiff tab labelled `HB_Utility_SD` "Posterior SD" under Stan, presenting a heterogeneity as a precision, which is the mislabelling F5 was ruled to end; the spread now carries one label on both paths and `HB_Mean_SE` reaches the island and the view as its own column. **A third, from running the demo**: `extract_hb_results()` is defined by BOTH maxdiff and conjoint in one global environment with different signatures, so the demo's Stan fit sampled for 471 s and then died on a missing `thin`; invisible until cmdstanr existed, because the EB fallback never calls it. maxdiff's is now `maxdiff_extract_hb_results()`, with a scan test guarding the class. `safe_numeric()` still collides across maxdiff, conjoint AND tabs, and is left for a platform decision. All three proved both ways. Verified: maxdiff 1083/0/1 skip/0, tabs 5255/0/1, the MaxDiff node gate 8/0, three Karoo runs (Stan exports unstamped with 400 rows summing to 100; `Generate_HB_Model = NO` refuses `MODEL_NO_RESPONDENT_UTILITIES`; the EB fallback refuses `MODEL_APPROX_UTILITIES`; both refusals now land as PARTIAL) and the integrated demo. Next action: Fable independent review of this branch's delta, then Duncan's eyeball and the merge.
- 2026-08-27 — **conjoint Session B2 + Session C implemented** (Opus, `feature/conjoint-v2-report`, off Session A with OPUS-0 merged in; not merged, not pushed). Conjoint now reaches the client's report two ways: per-respondent attribute importance exported as a tabs **Allocation** question (crosstabbable, weighted and significance-tested by tabs, with an integration proof driving tabs' own processor), and a frozen `data-cj` island + `27x_conjoint.js` view giving the v2 report a **Conjoint tab** (importance, part-worth utilities with the post-A4 honest intervals, model fit, WTP). **The review's §4 row-kinds premise was superseded**, with the argument recorded in `SESSION_C_NOTES_CONJOINT.md`: `build_dl_question`/`classify_row_labels` serialise crosstabs, and a part-worth has no banner, no base and no percentage — the same conclusion keydriver, catdriver and segment each reached independently on 2026-07-11. Zero new row kinds. Also superseded: Session B's P1-P8 report fixes, because the classic **tabs** report was retired 2026-08-05 (`2fdcb38f`) and conjoint's own report is the same architecture, which the migration plan §2 already calls throwaway. The review's named open item is settled: **no JS consumes individual betas and they were never embedded** — the §6 file-size hazard was about the dead html_simulator twin. Island is 3.6 KB. Nothing changes for a tabs report without conjoint (`cj_json` defaults NULL, tab appears only when TR.CJ has content); tabs suite went 5114 → 5144 passing, 0 failures. **Both remaining items done the same day:** the simulator was extracted whole into `lib/html_simulator/` (90 KB, self-contained, browser-verified computing shares) and `modules/conjoint/lib/html_report/` was deleted — 7 R + 7 JS files — with `generate_html_report` retired loudly through `CONJOINT_RETIRED_SETTINGS`. A conjoint run now produces: the Excel workbook, the stats pack, a `_cj_island.json` giving the interactive report a Conjoint tab, an optional crosstabbable importance export, and an optional standalone simulator. Next action: Fable pre-merge reviews in dependency order — Session A, OPUS-0, then this delta.

- 2026-08-27 — **OPUS-0 implemented** (Opus, `feature/opus0-socket-consolidation`, not merged, not pushed). Socket only, as scoped: one shared Kish n_eff (`modules/shared/lib/effective_n.R`), one shared min-base predicate (`modules/shared/lib/disclosure_gate.R`), and the pin script islands hardened. Three of the handover's premises did not survive contact with the code and are corrected in `OPUS0_NOTES.md` — most importantly, the confidence Kish copy was **not** verbatim identical to tabs (2.2727 vs 2 on `c(1,1,3)`), so consolidating under one name would have silently moved one module's numbers; the shared file now offers the fractional statistic and a rounded form, each named for what it is. maxdiff's copy re-pointed and proved inert on real data while fixing two NaN cases. Two findings for other sessions: `calculate_deff` means `1 + CV squared` in confidence and `n / n_eff` in maxdiff — two statistics, one name, not consolidated because unifying them moves numbers; and `simulator_pins.js` is a dead 461-line second pin engine nothing loads. `report_hub/js/hub_pins.js` was found to write its own island and carry the identical vulnerability — hardened, though the handover had it out of scope. Suites, all eleven modules, 0 failures: shared 903, tabs 5114, confidence 1063, catdriver 673, brand 2155, keydriver 940, maxdiff 797, tracker 1969, pricing 845, segment 1026, report_hub 360 passing. **This unblocks the five gated Session Cs.** Next action: Fable pre-merge review of this branch, then Duncan merges.

- 2026-07-10 — maxdiff review + handover written (Fable). FAB-0 verified. Standing decisions D1-D6 locked. Programme doc created. Next action: Duncan launches Fable reviews for the other five modules (any order; segment last is sensible given its parked-v2 history needs the most reconciliation), and/or starts maxdiff Session A in Opus — Session A is not blocked by anything.
- 2026-08-27 — conjoint **Session A implemented** (Opus, `feature/conjoint-correctness`, one commit per work item, not merged, not pushed). All of A1-A14 done with tests; the suite's 12 structural skips are gone (0 fail / 0 skip / 2 pre-existing warnings) and the skips turned out to be hiding real breakage — the HB tests errored the moment they were allowed to run. C1, C2 and C3 fixed; C3's posterior-SE route was verified empirically before it was written (mixture mean matches colMeans(individual_betas) to ~0.01; posterior SE falls 0.276 → 0.126 as n goes 30 → 120 while heterogeneity SD holds near 0.4). Nine findings not in the review doc are logged in `SESSION_A_NOTES_CONJOINT.md`, three of them client-facing: every confidence interval on the default aggregate path was NA (calculate_ci inherited the level name onto its own names); C3 was live a second time in build_latent_class_result; and six "Excel may repair this file" warnings fired on success branches, drowning the real one. Deviation from locked decision 7: the optimizer functions and predict_shares_with_ci all have working tests, so the decorative *config surface* was deleted and the functions kept — Duncan's ruling wanted, nothing depends on it. Next action: **Fable independent pre-merge review** of the branch (fresh session, no shared notes), then Duncan regenerates via launch_turas() and merges. Session B is unblocked after that.
- 2026-07-11 — conjoint review + handover written (Fable, §2 method: 4 readers + suite + coordinator verification of every CRITICAL). Key corrections to programme assumptions: conjoint HB = bayesm (not ChoiceModelR/Stan; PROG-1 narrowed to maxdiff), and V2_MIGRATION_PLAN's "standalone" conjoint simulator is actually embedded in the combined report (deprecated forwarder + dead twin dir). 3 CRITICALs, 12 HIGHs; suite 0-fail but with structural skips hiding the HB/LC/BW paths. Locked decisions in the handover §0 (Duncan may veto): BW-simultaneous refuses; HB SE fixed from posterior draws; export gate = HB/LC only; importance-Allocation flagship payload, per-level Numerics deferred; dead surface deleted. Next action: Duncan rules on review §8 items, then Opus Session A (`feature/conjoint-correctness`) — not blocked by anything; remaining reviews: pricing, keydriver, catdriver, segment.

- 2026-07-11 — pricing review + handover written (Fable, §2 method: 4 readers + suite + coordinator re-verification of every CRITICAL and load-bearing HIGH). 3 CRITICALs, 8 HIGHs + 3 report-layer HIGHs; suite 0/0/63 but structurally green-biased. Key programme corrections: pricing's simulator is EMBEDDED in the consolidated report (standalone builder is test-only dead code — second module where V2_MIGRATION_PLAN's simulator premise is wrong); plan's pricing row kinds would fight the banner-axis row contract — locked decision: **no v2 view for pricing**, tabs integration instead (VW/monadic questions declared tabs-side; GG grid + WTP exported D5-stamped). `psm_analysis_weighted` + `survey` verified installed (no environment blocker for the C1 fix). Locked decisions in handover §0 (Duncan may veto): weighted VW via psm_analysis_weighted with refusal-not-fallback; sequential GG refuses by default with explicit imputation opt-in; monotonicity default → drop with honest semantics; standalone simulator fork deleted. Next action: Duncan rules on review §8 items, then Opus Session A (`feature/pricing-correctness`) — not blocked by anything; remaining reviews: keydriver, catdriver, segment.

- 2026-07-11 — keydriver review + handover written (Fable, §2 method: 4 readers + suite + coordinator re-verification of every CRITICAL and load-bearing HIGH — including an independent numeric re-derivation of the relative-weights defect and an empirical template-load repro). 2 CRITICALs, 12 HIGHs; suite 0/2/4/928 but structurally green. Key programme corrections: V2_MIGRATION_PLAN's "keydriver reuses the `index_scores` row pattern" (:41) and its keydriver row-kind instruction (:55) are both false-to-code — `index_scores` is a microdata mean-recompute map and keydriver has no microdata; the correct route is a tracking-style frozen island + own workspace view with zero new row kinds (fourth §3/§4 premise across three modules that failed contact with code). Classic-tabs integration ruled out (aggregate-only module — first module in the programme with NO tabs-exporter phase). Good news: the headline Shapley column is mathematically correct — keydriver's problem is its second-billed method and everything around the engine. Locked decisions in handover §0 (Duncan may veto): RW fixed not dropped; dead v10.3 engine deleted + honest provenance stamps; no tabs exporter; TR.KD island route; no sig letters on importance (bootstrap CIs where they exist, "no interval" stamp on Shapley); template fixed loader-side (shared template_styles.R serves 9 other modules — untouchable). Next action: Duncan rules on review §8 items, then Opus Session A (`feature/keydriver-correctness`) — not blocked by anything; remaining reviews: catdriver, segment.

- 2026-07-11 — catdriver review + handover written (Fable, §2 method: 4 readers + suite + coordinator re-verification of every CRITICAL by execution — numeric LR repro, subgroup-schema repro, refusal-signature repro, template raw read). 3 CRITICALs, 11 HIGHs, 12 MEDIUMs + report-layer P-tier; suite 0/4/1/659 but structurally green (two masking mechanisms found: subgroup tests never construct the production schema, and the test helper's sorted source order binds the *correct* refusal signature while the GUI's order binds the stale one — the suite tests a different function binding than production runs). Key programme corrections: the §4 row's "SHAP via shapr" extra focus was **void** — catdriver contains no SHAP code at all; the claim is stale platform-doc drift (launch_turas tile, CLAUDE.md, README), third false programme premise found by review. Landing zone defined (module was absent from V2_MIGRATION_PLAN scope): NO tabs exporter (fitted probabilities are D5-inadmissible model estimates and redundant — sharper reason than keydriver's), frozen `TR.CD` island + own view riding TR.KD's scaffolding, zero new row kinds. New cross-module finding: shared `modules/shared/js/turas_pins.js` JSON island is not `</script>`-hardened — a hostile insight text kills ALL pins in every TurasPins module's saved reports; handover B5 scopes the shared fix with cross-module suite runs. Good news: the coefficient-to-level mapper (09_mapper.R) is the most rigorous piece of engine plumbing seen in the programme so far, and unweighted binary/ordinal runs are defensible. Locked decisions in handover §0 (Duncan may veto): multinomial_mode amputated to baseline_category; weights normalised + deff-stamped (no survey-package rebuild); equal-importance fallback refuses; no tabs exporter; TR.CD-after-TR.KD route; dead code deleted (~3,700 lines incl. 1,183-line never-called preflight validators). Next action: Duncan rules on review §8 items, then Opus Session A (`feature/catdriver-correctness`) — not blocked by anything; remaining review: segment (last, per its parked-v2 reconciliation needs).

- 2026-07-11 — segment review + handover written (Fable, §2 method: 4 readers on Opus subagents + suite run + coordinator verification of both CRITICALs, one by execution — token-lean coordinator run). **All six module reviews are now DONE.** 2 CRITICALs, 3 HIGHs, 8 MEDIUMs — the best-shape module of the six: template loads, preflight wired, save-a-copy safe, live engine math sound; its CRITICALs are dead-on-arrival features (mini-batch >10k crashes on a wrong call signature; LCA a config-exposed silent no-op), not wrong-numbers-under-PASS. Key programme corrections: the §4 row's "assignment-file bridge" extra focus resolved — NO bridge exists (zero repo consumers of `Segment_Assignments`; manual merge today), and Session C builds it; V2_MIGRATION_PLAN :41/:55 both false for segment (assignment is live tabs-native microdata → segment-as-banner, no island, zero new row kinds — the plan's per-module §3/§4 premises have now failed contact with code for 4 of the modules checked). Parked `feature/segment-report-v2` reconciled: stays parked, salvage list in review §7b; its own PLAN.md independently reached the same segment-as-banner division of labour. Locked decisions in handover §0 (Duncan may veto): LCA + ensemble amputated not wired; mini-batch fixed; non-kmeans scoring refuses; tabs exporter built with D5 stamps; no v2 view; parked branch never merged; TurasPins fix stays with catdriver B5. Next action: Duncan rules on any vetoes, then Opus Session A (`feature/segment-correctness`) — not blocked. Programme state: all six reviews + handovers done; every module's Session A is unblocked and independent; OPUS-0 gates only the maxdiff/keydriver/catdriver v2 sessions (conjoint C too); pricing and segment have no v2 session at all.

- 2026-07-12 — **brand review + handover written** (Fable, §2 method: 4 Opus readers + suite ×2 + coordinator verification of both CRITICALs — Dirichlet verified by executing NBDdirichlet and inspecting the object, save-loss by direct JS read; token-lean coordinator pattern). Brand added to the programme (was "own roadmap"). 2 CRITICALs, 5 HIGHs, 9 MEDIUMs, 3 LOWs — second-best-shape module after segment; its CRITICALs are a dead-on-arrival flagship analytic (Dirichlet expected values never once computed — all NA under PASS since the extractor probes fields the package object doesn't have) and a work-destroying report bug (Save discards analyst commentary; three divergent persistence stores, none serializable). Key programme findings: brand is a **fourth integration archetype** — v2-standard upgrade in place (no exporter, no island, no new row kinds; V2_MIGRATION_PLAN needs a brand *addendum*, not a correction — first module with no plan premise to falsify); brand is the first of seven with NO GUI success-on-refusal and NO template-load defect; its H2 (sig without Kish n_eff despite weighted estimates) is the programme's clearest OPUS-0 consumer case. Parked v2-upgrade branch reconciled (doc still-true; merge docs-only commit 6c1f0e41, close branch); stale memory corrected (`feature/branded-reach-and-dba` already in main). Locked decisions in handover §0 (Duncan may veto, review §9): Dirichlet refuse-on-NA; weighted penetrations + on-face denominators; single serializable persistence store; no exporter/no view; Kish + disclosure gate + brand_pins retirement gated on OPUS-0; Rec A clarity-IA excluded as a separate Duncan-scoped effort. Next action: Duncan rules on review §9, then Opus Session A (`feature/brand-correctness`) — unblocked. Programme state: seven reviews done at this point; weighting followed same-day (below).

- 2026-07-12 — **weighting review + handover written** (Fable, token-lean: 4 Opus readers + suite run + coordinator verification of the load-bearing claims by direct read/grep — trimming path, dead rescale, un-normalised design pipeline, missing duplicate-ID guard all Fable-verified). Calculations-only scope per Duncan (reporting side out). 2 CRITICALs / 4 HIGHs / 7 MEDIUMs; core engines verified sound (survey::calibrate raking, correct row alignment, exact Kish). Single-session handover W1-W8, branch `feature/weighting-calc-correctness`; 4 §0 decisions await Duncan. **Programme review phase now fully complete: 8/8 modules (6 original + brand + weighting).** Next action across the whole programme: Opus Session As — all eight unblocked and independent. **Session As proceed on each handover's locked §0 defaults; Duncan's veto, if any, wins** (rulings are only hard gates where a handover explicitly says so).

- 2026-09-03 — **pricing blindspot pass + handover refresh** (Fable). Verified on main `3f85abb3`: nothing from the July review was ever built (Session A never started; C1 to C3 and H1 all live; suite 0/0/63, the July baseline). The July handover was stale on four points: its locked decision 4 (no v2 view) rested on the row-kind route, since disproved by conjoint and maxdiff; its Session B patched the classic pricing report, which is the generation retired on 2026-08-05; its decision 5 kept the simulator embedded against D2 and both peers; it named Opus 4.8. New brief `HANDOVER_PRICING_V2_FOR_OPUS.md`: keep the July Session A work orders, add a Karoo pricing example as A's first deliverable (no client pricing project exists to eyeball), then a v2 session on the maxdiff template (island, Pricing tab, GG grid Multi_Mention export + pricing_valid, standalone simulator, classic report retired, integrated demo). Live VW under the audience filter is feasible (a pure function of four per-respondent numerics already in TR.MICRO) but parked behind the allocation live-recompute work and a parity gate. Session A started the same day on `feature/pricing-v2-lift` in a worktree.

- 2026-09-03 (later) — **pricing Session A built** (Fable, `feature/pricing-v2-lift` in a worktree, notes `SESSION_A_NOTES_PRICING.md`). Karoo pricing example first (four configs, run headless through `run_pricing_analysis()`; the template-shaped config reproduced H1 before any fix). Then the July work orders A1 to A9: weighted VW through `psm_analysis_weighted()` with refusal-not-fallback, bootstrap around the reported estimator, stop-early GG refusal with `NO_AFTER_STOP` opt-in, strict binary coding with `ONE_TWO`, PAVA smoothing, monadic weights normalised with a p-value caveat, Kish from the shared helper, stats pack switch honoured, retired settings, Min_Sample enforced, dead output blocks removed, template regenerated. Seven findings beyond the review (ties, validate=FALSE counting, the monadic re-source, glm noise, the stats pack declaring weighted runs unweighted, `$` in segment insights, a self-inflicted generator truncation repaired from git). suite 997 pass / 0 fail / 0 skip / 0 error / 14 warnings (63 before). Next: Fable pre-merge review, Duncan's eyeball on the Karoo example through `launch_turas()`, then Session B.
- 2026-09-03 (evening). **Pricing Session A independent review** (Fable, `REVIEW_FINDINGS_PRICING_SESSION_A_2026-09-03.md`). Verdict PASS with fixes. Suite reproduced at 345 tests / 997 expectations / 0 failed / 0 skipped / 0 errors; the three new test files run against a `3f85abb3` worktree fail or error in 38 of 39 blocks (the exception is the PAVA unit test, whose function pre-existed unchanged). C1: weighted ECDF equals the hand-computed weighted share at four prices and integer weights reproduce row duplication to grid resolution. C2: Karoo stop-early refuses, imputation stamps itself; the R100 to R80 shift on Karoo is a fixture artefact (71 of 400 have a Yes above their first No in the noisy full ladder). C3: estimate column equals the headline, and the equal-probability bootstrap is wider (OPP SE 2.15 vs 1.93) and centred closer to the headline than the old weighted-probability one. Found: long-format Gabor-Granger keeps H5 (1/2 data becomes 100% intent after following the DATA_GG_UNEQUAL_BASES fix text); the Results workbook Summary sheet still prints n valid as Effective Sample Size (H7 fixed in the stats pack only); the NMS extension fails with the template's `PI_Scale = 5` and its weighted path disagrees with the unweighted one on unit weights; the 2% rung-base tolerance refuses 25 to 50% of full-presentation ladders at 2 to 3% item non-response on n=300; under drop the stats pack says violation rate 0.0% while 44 were excluded; the base check is skipped under NO_AFTER_STOP; `$` hardcoded in the price ladder notes. Classic HTML report and Excel workbook rendered from the new objects without breaking; stats-pack switch verified from config and GUI option both ways; template identical to a fresh regeneration. F2 fixed with a test on `fix/pricing-review-a-followups` (`e8315e66`, off main at `0698ffd2`), unmerged, unpushed; suite there 346 tests / 1005 expectations / 0 failed. Next: Duncan reads the findings, a small follow-up session lands F1, F3 to F12, then his Karoo eyeball; Session B unaffected.


- 2026-09-04. **Pricing Session B built and merged** (Fable, `feature/pricing-v2-report`, four commits, since merged and pushed). The v2 island, the Pricing tab and its node gate, the tabs export, the standalone simulator, the classic report withdrawn rather than retired, and pricing added to the integrated demo. Two tabs contract facts the July brief got wrong were proved by executing against tabs' own code and are now pinned by tests: a `Multi_Mention` counts a mention by comparing each CELL to the option's `OptionText`, so a grid of 0/1 flags reports zero at every row; and it looks options up with `^{code}_[0-9]+$` against the Options sheet, so option rows are keyed by column, not by the question code. The demo was rebuilt and read back before the session closed. Review F2 from Session A went in separately as `7fbfa461`.

- 2026-09-04. **Simulator minify coverage audited, built and merged** (`f444368b`, `d0bd34e2`; `docs/disclosure/FINDINGS_simulator_minify_coverage.md`). The `srcdoc` hole is maxdiff-only, but conjoint, the maxdiff standalone simulator and brand were all shipping unminified for a different reason: their deliverable checkbox had no call site, conjoint's having been deleted with the retired HTML report in `fc27160a`. Wiring fixed for all three; the srcdoc hardening landed inside `turas_minify()` as step 2b, on the deliverable path only, because hardening it where the report is written also hardened the `_dev` copy.

- 2026-09-16. **Status check across the whole programme**, read from the code and from git on main `9df7b044` (level with origin). Conjoint COMPLETE. MaxDiff merged and reviewed, with its follow-up handover unrun and the classic-report-to-v2 work not started. Pricing merged, with five review-A follow-ups still live and Session B never independently reviewed, which is the only outstanding review in the programme. Keydriver, catdriver and segment have still never started build: the last commits touching those modules are the August workbook-saver pass and the September deliverable-gate commit, neither of them Session A work. Their July CRITICALs (keydriver's relative-weights math, catdriver's weighted multinomial importance, segment's dead mini-batch k-means and no-op LCA) are therefore all still live. That is the bulk of what remains.


- 2026-09-16 (later). **Pricing Session A review follow-ups built** (Opus, `fix/pricing-review-a-followups-2`, eight commits, unmerged). F1 long-format binary domain check and a refusing coder; F3 the NMS extension refused by name with PI_Scale no longer passed and five documents updated; F4 and F6 the missingness-shape rule, so scattered item non-response excludes and discloses while a stop-early ladder still refuses; F5 the violation count carried through the handling it describes, into the stats pack and the Pricing tab; F7 to F12 the six small ones. Every new test was run against the code as found and 22 of 25 fail or error there. The completeness rule broke the tabs export's `pricing_valid` base on the way through, caught and fixed in the same session. Duncan's eyeball on 2026-09-17 passed, with the stop-early config still refusing, and found a `diagnostics$warning` field that had never been read by anything; it prints now. Owed: Duncan's eyeball of a regenerated Karoo workbook, then the merge.

- 2026-09-17. **Pricing Session B independent review** (Fable, `REVIEW_FINDINGS_PRICING_SESSION_B_2026-09-17.md`, uncommitted). Verdict BLOCK on the tabs export and on Present mode, PASS WITH FIXES on the island, the tab and the withdrawal, scoped to what the session executed itself. Suites from the repo root: pricing 379 tests / 1,092 expectations / 0 failed / 0 skipped; tabs 1,741 / 5,945 / 0 failed / 1 skipped (a tracking_island skip); 45 node gate files, 1,157 passed, 0 failed (the author's 41 was the count at `089ef7d2`). Five scratch Karoo runs, none under `examples/`. Hand recomputation reproduced the island's GG acceptance, revenue and profit indices, arc elasticity, VW points and intervals, Kish n_eff, method spread and monadic cells, and the tab rendered against real islands with no NaN, dollar sign or em dash. F1 (CRITICAL): a third tabs contract fact the author missed: the Multi_Mention base is respondents with at least one mention (`weighting.R:524-559`), so "would not buy at any price" respondents leave the crosstab base and every rung share reads higher than the module's (Karoo 354 of 356 in base, 94.4% vs 93.8% at R60; the shipped demo 520 of 524; the shipped stop-early-imputed config 100.00% at R60 in tabs against 93.77% published, by construction, because under NO_AFTER_STOP a first-rung No is an all-blank row), the export's METHOD sheet and console note say the opposite direction, and its advice to filter on pricing_valid is a no-op because the dropped rows are never invalid. F15 (HIGH): a long-format ladder refuses the export as duplicate ids. F16 (MEDIUM): run_result is snapshotted at `00_main.R:539` before the island, export and simulator steps, so a refusal in any of them still ends at TRS PASS. F17 to F20: the snippet's sheet name and columns, the empty-currency CSV path, DisplayOrder as text at 10+ rungs, and two tests pinning the false wording. F2: the `.pr-*` classes the Pricing tab added to `styles.css` collide with Present mode's, verified in headless Chrome; every report on main since 2026-09-04 has Present mode restyled. F11 (HIGH): a VW-only run with `Generate_Simulator = TRUE` writes the island before step 9 refuses for lack of a curve, so `meta.simulatorFile` names a file that does not exist and `test_v2_island.R:311` pins it. F12 (HIGH, inherited): Simulator-sheet scenarios never reach the page. Ten smaller findings on wording, a self-comparing byte-identity test, and fields carried but not drawn. Not covered by the coordinator's own execution: the simulator beyond F11 and the export's coding rules and WTP (the readers' checks stand as their claims), the demo rebuild and the follow-up interaction (that reader stopped without findings), the GUI run, and the author's notes. Fix record to follow separately.

- 2026-09-17 (later). **KeyDriver Session A independent review** (Fable, `REVIEW_FINDINGS_KEYDRIVER_SESSION_A_2026-09-17.md`, uncommitted). Verdict PASS WITH FIXES, 0 CRITICAL, 7 HIGH, 12 MEDIUM, 10 LOW. Both CRITICALs verified by execution in both directions against independent implementations written from the definitions: relative weights agree with an independent Johnson to 1e-14 and sum to the weighted R-squared (old engine 24.6 points off, 50/50 on two drivers); weighted dominance agrees with a brute-force weighted LMG to 6e-17 (old transform summed to 0.901 against 0.743); the segment layer is weighted, scores factor drivers (22.5 where the old code wrote 0.0) and reads every Segments row. The shared loader is safe on every sourcing shape in the repo and fixes the nested-function case that refused on main; branch history clean of the rescued commits. The HIGHs are all in the layer around the engine: SHAP on a factor driver still fails (the dummy map is handed to shapviz inverted), pre-flight runs only when the caller sources `lib/validation` and check 13 refuses a template-spelled quadrant config that runs clean under "Yes", check 5 degrades template-built mixed studies with a false `partial_r2` message, an unweighted mixed study with one numeric driver still dies with a bare error, an all-categorical study is refused with advice naming a setting that does not exist, and `random_seed` never reaches the bootstrap. The example fixture is right at its committed seed; across 20 seeds Shapley swaps the two smallest drivers in 3 of 40 runs, so the README's "noise cannot reorder them" overstates the design. Suites from the repo root: keydriver 395 / 1,202 / 0 failed / 2 skipped; shared 465 / 1,348 / 0 / 4; catdriver 227 / 676 / 0 / 1. Next action: the seven HIGHs on the branch with tests that run the production path, then Duncan's `launch_turas()` eyeball and merge.

- 2026-09-19. **CatDriver Session A independent review** (Fable, `REVIEW_FINDINGS_CATDRIVER_SESSION_A_2026-09-19.md`, committed on `feature/catdriver-correctness`). Verdict PASS WITH FIXES for the merge and BLOCK on two paths (multinomial subgroup comparison, ordinal probability lifts), 2 CRITICAL, 3 HIGH, 16 MEDIUM, 9 LOW. Four independent readers (weighted likelihood machinery; refusal and guard layer in GUI source order; subgroup comparison, output sheets and the unweighted regression; test-suite honesty) plus coordinator re-derivation of every CRITICAL and load-bearing HIGH by execution in both directions, against a detached worktree of main and independent implementations written from the definitions. All three July CRITICALs and H1, H2, H5 are closed and the numbers are right: weighted multinomial importance matches an independent weighted log-likelihood LR share to 4e-4 (main: -212 to -242, all 0 per cent, p = 1, PASS); weighted McFadden matches an independent weighted null to four decimals for clm and multinom; a fivefold weight rescale moves nothing on the branch and shrank every SE by 1/sqrt(5) on main; the subgroup comparison builds and matches an independently assembled table cell for cell; a bad config refuses cleanly where main said BUG_INTERNAL_ERROR; the weighted binary bootstrap produces 200 of 200 intervals where main produced none. Unweighted binary and ordinal are identical to main. What the session got wrong is its own subject, provenance: the clm importance stamp names the wrong statistic (car::Anova on clm is Wald; 139.8 versus LR 203.6); an unusable weight column runs unweighted under PASS with the Declaration saying weighted; the multinomial subgroup OR table overwrites outcome levels (CRITICAL on that path); the ordinal lift is P(observed category) with a new label calling it a lift (CRITICAL on that path, main identical); the Declaration swaps received and analysed. Three readers were cut by an account rate limit mid-run and replied from their transcripts; their scripts and logs are on disk and the coordinator re-ran the load-bearing items. Author's notes read after section 4: the one outright contradiction is the blanket claim that every fix was watched failing, which held for the author's probes and not for the suite (H1 and M1 revert green). Suites: catdriver 278 / 921 / 0 / 1; shared 469 / 1,353 / 0 / 4; main 227 / 676 / 0 / 1. Next action: the five HIGHs and F6 on the branch with tests that fail on the current code, then Duncan's `launch_turas()` eyeball and merge.

- 2026-09-21. **Segment Sessions A, B and C independent review** (Fable, `REVIEW_FINDINGS_SEGMENT_2026-09-21.md`, committed on `review/segment-v2-lift` off main, not merged, not pushed). Verdict MERGE AFTER FIXES, no revert, 3 HIGH (one fixed), 6 MEDIUM (two fixed), 6 LOW (two fixed), 5 OBS. Method: detached worktree of the merge base and one probe script run on both sides for every CRITICAL, HIGH and executable MEDIUM (mini-batch dies with the July review's exact error at the base and clusters 12,000 rows through the whole pipeline now; `use_lca = TRUE` validated silently and is refused now; GMM models scored by the k-means rule at the base and refuse now; dropped settings carried; the comparison table threw at the base and builds now); three independent tracks: 23 mutations against the full suite (four fixes unguarded, now guarded), the worked example in every mode plus the export with NAs, outliers and a blank row plus a real `run_tabs_analysis()` on the exported file (bases 1200 | 532 | 263 | 405), and the Shiny server under `shiny::testServer` (refusal path correct; a PARTIAL run was announced as a clean success because the classifier read `run_status` where the engine writes `status`, fixed). Found in passing, pre-existing: `demographic_vars` and `html_show_demographics` are parsed, offered and computed by nothing (`profile_demographics()` has no caller), and `k_selection_metrics` advertises three uncomputable metrics. Duncan's rulings in the review's section 6. Segment and tabs suites at `f2a03514`: 1,185 / 0 / 0 / 0 and 6,215 / 0 / 1 / 0. Later the same day Duncan ruled on three of the five (export semantics, checkbox, CH and DB wired in) and they were applied on the branch in `ebc04f93`, suite 1,258 / 0 / 0 / 0; F2 and F14 remain his.

- 2026-09-21 (later). **Segment Session D commissioned** (Fable): `HANDOVER_SEGMENT_D_FOR_OPUS.md` (demographic profiling wired end to end per Duncan's ruling on F2; `tabs_export` refused in combined mode per F14; three LOW wording items) and `REVIEW_BRIEF_SEGMENT_D.md` for an independent Opus review, both on `review/segment-v2-lift`. Opus 5 at high effort for both; Fable not needed.

## 6. Review-session brief (template for the pending reviews)

Paste to a fresh Fable session:

> Read `docs/v2_lift/V2_LIFT_PROGRAM.md` §2 and follow the pipeline's step-1 method for the {module} module: four parallel independent readers (statistical engine; config/UX/doc-drift; report gap-vs-tabs-v2 + report bugs; tabs/v2 integration feasibility per `modules/tabs/docs/V2_MIGRATION_PLAN.md`), plus run the module's test suite and spot-verify every CRITICAL claim yourself before reporting. Extra foci for this module are in the §4 tracker row. Deliverables, all in `docs/v2_lift/`: `{MODULE}_PRODUCTION_REVIEW_{date}.md`, a locked-decisions handover `HANDOVER_{MODULE}_FOR_OPUS.md` modelled on the maxdiff one, and updates to the §4 row and §5 log. Review only — no code changes. No fabrication: every finding carries file:line evidence read this session; unverified claims are labelled.
