# Independent pre-merge review: brand v2 lift (`feature/brand-v2-lift`)

**Date:** 2026-09-05.
**Reviewer:** Fable session, independent of the fixing session. Its notes (`SESSION_NOTES_BRAND_2026-09-03.md`) were treated as claims to test, never as evidence. Its chat was not read.
**Reviewed:** worktree `/Users/duncan/Dev/Turas-brand-v2-lift`, branch tip at the start `7c7a13ae` (18 commits on merge base `3f85abb3`). Local main has since moved to `ca27c506`, so `git diff main..HEAD` shows tabs files that are not this branch's; every scope statement below is against the merge base.
**Brief:** `docs/v2_lift/REVIEW_BRIEF_BRAND_V2_LIFT.md`.
**Method:** verdict by execution. Every CRITICAL and HIGH claim was run both ways where the old code could be sourced (fails or gives the wrong number on `3f85abb3`, passes on the branch). Three verification tracks ran as subagents briefed as independent (browser, engine, config/GUI/wording); every finding of theirs that changes a row below was re-verified by me against the code or by rerunning the probe, and rows that rest on a track's evidence alone say so. "Read" in a How-verified cell means read-level evidence only.
**Fixes committed on the branch by this review:** `e8dac4d3` (Dirichlet nstar convergence) and `e9cd638d` (eight follow-ups). Not merged, not pushed.

## 0. Verdict

**MERGE AFTER FIXES. The fixes are already on the branch; nothing else blocks.**

The branch does what its notes say, with one exception the notes could not have seen: the C1 Dirichlet fix read the expected values correctly but from a purchase-count distribution the package itself flagged as truncated. On the IPK fixture that moved every expected SCR by four to six points of deviation and flipped two of fifteen Double Jeopardy flags, a wrong number under PASS by the brief's definition. That is fixed in `e8dac4d3`. A second batch (`e9cd638d`) closes seven smaller defects found by the tracks and confirmed here, one of them a report-layer denominator misstatement in a new caption.

Before merging, Duncan should regenerate from `e9cd638d` or later and look at three things listed in section 6. The suite at `e9cd638d` is FAIL 0, WARN 1, SKIP 2, PASS 2399.

## 1. Gates

| Gate | Command | Result |
|---|---|---|
| Brand suite, tip `7c7a13ae` | `TURAS_ROOT=<worktree> Rscript -e 'testthat::test_dir("modules/brand/tests/testthat", reporter = ProgressReporter)'` | FAIL 0, WARN 1, SKIP 2, PASS 2360 (79.7 s). Matches the notes. |
| Brand suite, `e8dac4d3` | same | FAIL 0, WARN 1, SKIP 2, PASS 2385 (87.7 s). |
| Brand suite, `e9cd638d` | same | FAIL 0, WARN 1, SKIP 2, PASS 2399 (82.8 s). |
| Scope | `git diff --name-only 3f85abb3..HEAD \| grep -v '^modules/brand/' \| grep -v '^docs/v2_lift/'` | empty at the tip; my two commits touch only `modules/brand/`. |
| IPK fixture end to end, tip | `run_brand()` on `modules/brand/tests/fixtures/ipk_wave1` then `generate_brand_html_report()` (`scratchpad/review/run_ipk.R`) | PASS, no warnings, 5.7 s, HTML 3.84 MB. Console shows the package's `nstar is too small! (nstar= 50 )` (F1). |
| IPK fixture, `e9cd638d` | same | PASS, 7.1 s, `nstar_used = 200`, DJ flags 11 on line / 3 over / 1 under. |

The two skips are the baseline's (mocked PKG path unreachable; on-CRAN timing guard). The one warning is the pre-existing `max` of empty in `test_ma_panel_cat_avg_mask.R:155`. Files that error in isolation: `test_html_report.R`, `test_cat_buying_panel.R`, `test_guard_and_config.R` as the brief says, and (new on the branch, now fixed) `test_portfolio_dop_aware.R` (F6).

## 2. Findings

Severity: CRITICAL blocks merge; HIGH should be fixed before merge; MEDIUM after; LOW is polish; OBS is context. "Fixed" means committed on the branch by this review.

| ID | Sev | File:line (branch tip) | Finding | How verified | Status |
|---|---|---|---|---|---|
| F1 | CRITICAL | `modules/brand/R/08c_dirichlet_norms.R` `.dn_call_dirichlet` (tip 197-221) | The C1 fix called `NBDdirichlet::dirichlet()` at the default `nstar = 50` and never read `dir_obj$error`. The package sets `error = 1` when the truncated NBD purchase-count distribution keeps under 0.99 of its mass or its mean drifts from M. On IPK Dry Seasonings (mean 10.1 purchases per buyer) the kept mass was 0.9945 and the truncated mean 8.66 against M 8.99. A converged fit moves expected penetration by up to 0.77 pp, buy rate 0.74, SCR 1.96 pp, 100% loyal 1.58 pp; every SCR deviation shifts four to six points; CHK (18.3% to 24.5%) and HND (18.0% to 24.1%) cross the 20% line and flip from on line to over. The comment called the notice "informational" and said "our guard validates the output"; the guard checked range, not convergence. | Executed: `c1_check.R` (engine inputs from the saved result, `dirichlet()` at nstar 50/100/200/400, flags recomputed), `c1_timing.R`. Both ways: the new test fails on the unfixed branch (3 failures) and passes after the fix. | Fixed `e8dac4d3`: ladder 50/100/200/400; accept when the package passes and kept mass reaches 1 - 1e-6 (past which values are stable to four decimals); package-accepted but short at the top of the ladder is PASS with a warning (element PARTIAL); package-rejected at the top refuses `CALC_DIRICHLET_NSTAR`; each attempt's `cat()` output captured; `nstar_used` in the result. IPK cost 5.7 s to 7.9 s. |
| F2 | OBS | `SESSION_NOTES_BRAND_2026-09-03.md` C1 row | The notes' proof "flags 13 on line / 1 over / 1 under" no longer reproduces: after F1 it is 11 / 3 / 1. The notes were right for the code as it stood; the number was the symptom. | Executed: per-brand table, `ipk_branch` versus `ipk_fixed` results. | Recorded; notes not edited. |
| F3 | HIGH | `00_guard.R` `.brand_coerce_weights` (tip 214-236); `00_main.R:476` | The new M8 message said "blank or NA rows are treated as weight 0" but the function returned NA for blanks. Mental Availability sums weights without `na.rm` (`02_mental_availability.R:168,214`) and `run_mental_availability()` is called outside any `tryCatch` (`00_main.R:476`), so a single blank weight cell gave NA MPen for every brand or failed the run. Pre-existing on main (which passed the raw column); the branch added a false claim on top. | Read (mine): the three sites. Executed (config track): `calculate_mpen()` with an NA weight returns NA for every brand. | Fixed `e9cd638d`: blanks become 0 in the coercer (what every `na.rm` element already did), `run_brand()` appends a warning naming the rows, status PARTIAL. Integration test: two blank cells give PARTIAL with "2 blank cell(s) ... rows 7, 120". **Duncan may prefer a refusal instead; that is a two-line change in the same function.** |
| F4 | HIGH (pre-existing) | `lib/html_report/99_html_report_main.R:125-131`; `00_guard.R` `brand_gui_outcome` | `transform_brand_charts()` fails on the IPK fixture on every run (`missing value where TRUE/FALSE needed`); the generator printed one line, returned an empty chart list and PASS, and the GUI announced success. Identical on main (read of `git show 3f85abb3:` same lines). Exactly the M2 class the branch set out to close. | Read (mine) both versions; executed: the line prints on every IPK generation this session. | Fixed `e9cd638d`: the generator returns the failure in `$warnings`; `brand_gui_outcome()` folds generator warnings into the verdict (PARTIAL, file path kept). **The transform bug itself is not fixed**; until it is, an IPK-shaped run will show one warning in Step 5. Follow-up: find the NA comparison in `01_data_transformer.R` `transform_brand_charts()`. |
| F5 | MEDIUM | `panels/08_cat_buying_panel_table.R:545` | New live caption said "Buy rate, volume share and SCR are among each brand's buyers." Volume share is the brand's share of all category purchase volume (`08c` `.dn_observed`: `vol = sum(w * x_mat[, bi])` over every respondent; `Share_Pct = Volume / total_vol`). A denominator misstated in a new reader-facing string. | Read (mine): both sites. Executed: the corrected caption renders in the `ipk_final` HTML. | Fixed `e9cd638d`. |
| F6 | MEDIUM | `tests/testthat/test_portfolio_dop_aware.R:15-22` | After M4, `09b_portfolio_dop_awareness.R` calls `.brand_meets_min_base()` but the test sourced only `09b`. In isolation on the branch: FAIL 10, 20 errors "could not find function"; on main in isolation it passed (engine track: 55 pass). The full suite hid it because earlier files load the guard. | Executed (mine): isolated run before and after. | Fixed `e9cd638d`: sources `00_guard.R`. Isolated run now PASS 55. |
| F7 | MEDIUM | `09b_portfolio_constellation.R:301` | The M4 rewrite dropped `base$n_uw == 0L ||` from the category gate. `meets_min_base(0, 0)` is TRUE, so at `portfolio_min_base = 0` a category with no qualifiers (BAK in the fixture) stopped being suppressed. Production default is 30, so only a hand-set threshold reaches it. | Read (mine): diff against main. Executed: new test with `min_base = 0L` expects BAK suppressed. | Fixed `e9cd638d`. |
| F8 | MEDIUM | `13c_al_classify.R` `.al_sig_two_props` (tip 172-196) | With H2 the Fisher branch received fractional `n_eff`, so the 2x2 held fractional cells and `fisher.test` printed "'x' has been rounded to integer" on every weighted Audience Lens run (engine track observed the warnings in a weighted call). | Read (mine). Executed: new test with fractional `n_eff` expects no warning, test = fisher. | Fixed `e9cd638d`: whole-count bases for the Fisher matrix only; the z branch keeps the fractional n. |
| F9 | MEDIUM | `docs/BRAND_CONFIG_GUIDE.md:230,232,234` | Three of the six newly documented keys described the wrong behaviour. `audience_lens_max` is a refusal ceiling (`13a:126-131`, `CFG_AUDIENCE_CEILING_EXCEEDED`), not a display truncation. `funnel_tenure_threshold` is a numeric cut-off on the tenure question (`03a`: `vals >= thr`; blank drops the stage), not an attitude code. `portfolio_cooccur_min_pairs` counts category buyers aware of both of two brands (`09b:340-345`), not cross-category buyers. | Read (mine) against the engine after the config track flagged them. | Fixed `e9cd638d`. |
| F10 | MEDIUM | `modules/brand/templates/` | The committed template workbooks were not regenerated after the generator changed: `Brand_Config_Template.xlsx` still carries `db_importance_method`, and no template's `MarketingReach` sheet has `SeenQuestionCode` / `BrandQuestionCode` / `MediaQuestionCode`. `templates/README.md` says the files are copies of the IPK fixture, which they are not either (the fixture's Settings has no `db_importance_method`). Six workbooks of mixed provenance. Consequence is bounded: the engine refuses with the missing columns named rather than mis-computing. | Read (mine): README, generator entry points, `git log` of the directory. Executed (mine): openxlsx read of the committed workbooks: `Brand_Config_Template.xlsx` Settings has one `db_importance_method` row and still describes `portfolio_extension_baseline` as "all, buyers"; `Survey_Structure_Brand_Config_Template.xlsx` has a MarketingReach sheet with no `SeenQuestionCode`; the other two structure workbooks have no MarketingReach sheet at all. | **Open, for Duncan**: decide which files are canonical, regenerate the two generator outputs (`generate_brand_templates(output_dir)`), fix the README. Not done here: a curation call, and regenerating committed binaries is his. |
| F11 | LOW | `panels/11_demographics_panel.R:318-321`; `panels/14_summary_panel.R` notes block | "Percentages are weighted" and "Weighted." were stated unconditionally, on unweighted runs too. | Read (mine); executed: `ipk_final` HTML now reads "unweighted" (IPK has no weight variable). | Fixed `e9cd638d`: conditional on `meta$weighted` / `config$weight_variable`. |
| F12 | LOW | `js/brand_summary_panel.js:455` | New missing-value placeholder was an em dash glyph. | Read. | Fixed `e9cd638d`: `n/a`. |
| F13 | MEDIUM (pre-existing) | `js/brand_audience_lens_panel.js` (`data-al-bound`), portfolio panel (`pfFpBound`) | Panels mark themselves bound through `dataset` (`panel.dataset.alBound = "1"`, `brand_audience_lens_panel.js:41-42`; `table.dataset.pfFpBound = '1'`, `brand_portfolio_panel.js:224-225`), which serialises as a `data-*` attribute. A saved copy carries it, so on reopen the binder returns early and that panel's handlers (Audience Lens clear button and sub-tabs; portfolio footprint controls) are not attached. Identical on main. Commentary itself still restores (the mirror is on the textarea, not the binder). | Mechanism confirmed (mine): code read, and `data-pf-fp-bound="1"` is present once in my dumped DOM; `data-al-bound` is absent there only because no Audience Lens panel rendered for the IPK fixture. The dead-handler behaviour on a reopened file is the browser track's observation, not rerun by me. | Open, follow-up: bind on a JS-side flag (or clear the attributes before Save). |
| F14 | LOW | `lib/html_report/03_page_builder.R:998-1030` | An inline script interpolates a section id and colour from config without escaping. Config-controlled, so low. | Browser track, read. Not re-read by me. | Open. |
| F15 | OBS | `panels/08_cat_buying_panel.R` `.cb_kpi_strip()`; `03c_funnel_panel_data.R` `base_note` / `significance_note` | Two M7 labels are inert: the `% Category buyers ... of N asked` chip lives in a renderer with no callers (grep: 0), and the funnel notes are written but never rendered (grep: no reader in panels or JS). The notes disclose both. The live cat-buying strip has no "% category buyers" chip, so no live surface is mislabelled. Also: `cb_norms_table_html()` has no callers and keeps a retained em dash in its footer. | Grep (mine). | Recorded; tidy-up candidate. |
| F16 | OBS | `SESSION_NOTES` late-fixes paragraph | Says two `<<-` were changed. One pre-existing site changed; the other was written new with `<-`. No code impact. | Diff read (mine). | Recorded. |
| F17 | OBS | Summary "Which penetration is which" on IPK | Shows two of three rows: `cat_buying_frequency$pct_buyers` is NA because the fixture's Options rows carry no Role. Data, not code. | Executed: RDS inspection. | Recorded. |
| F18 | OBS | `00_guard_role_map.R` `guard_alchemer_parser_shape` | The new "X columns with data" rule would refuse a parsed export whose real question codes are `X1..X5` with data. Unlikely naming; noted. | Read (config track and mine). | Recorded. |
| F19 | OBS | `run_brand_gui.R` Step 5 card; package guard | Step 5 prints the engine status ("PASS") beside a generator problem. The package-guard refusal list, if it ever fired, would reach `shiny::runApp` as a non-function; unreachable in practice because the hub is itself Shiny. | Config track, read. | Recorded. |

## 3. Claims in the brief, one by one

| # | Claim | Result | Evidence |
|---|---|---|---|
| 1 | C1 Dirichlet expected values from the package closures, finite, refusal on NA | **Holds, with F1.** `.dn_extract_expected()` equals a direct `dirichlet()` call at the same nstar to four decimals on all 15 IPK brands (max abs diff 0 across pen, buy rate, SCR, 100% loyal). The 100% loyal formula (sum over n of Pn(n) p.rj.n(n, n, j), over brand.pen(j)) and penetration were rederived from closed forms (NBD mass via lgamma; beta-binomial all-n and none-n products) on a 3-brand toy at nstar 6: max diff 3.6e-14 and 1.1e-13. Main's `08c` on the known fixture: every expected column NA under PARTIAL, all flags on line. Single-brand crash unreachable: `n_brands < 2` refuses `DATA_SINGLE_BRAND` before the fit (`08c:50`). IPK flag distribution after F1: 11 / 3 / 1. | `c1_check.R`, `c1_main_side.R`, `c1_timing.R` (all under `scratchpad/review/`) |
| 2 | C2 and H3 commentary persistence | **Holds, executed in headless Chrome by me on the `e9cd638d` IPK report.** One textarea of each class typed into via `input` events (`.br-insight-editor` 13 found, `.fn-insight-textarea` 2, `.ma-insight-box-text` 5, `.brsum-insight-editor` 1); DOM dumped (4.1 MB); each marker sits inside its textarea's text content; the dump reloaded and every value read back. A box typed into then cleared reads back empty. A value set without any event was captured by `_brSyncAllCommentary()`, the call `_brSaveReport` makes before reading `outerHTML` (`brand_report.js:210-213`, read). Main side (read): old `brand_ma_panel.js` used `sessionStorage`, old `brand_audience_lens_panel.js` used `localStorage` keyed `turas_al_insight_ + cat`, old `_brSaveReport` had no sync. `test_section_insights.R`: FAIL 0, PASS 96 (my run). Not exercised by me: a Section_Insights pre-fill in the browser (the IPK fixture was not checked for that sheet). Related: F13. | `scratchpad/review/c2b/{a_src,A,b_src,B}.html` |
| 3 | H2 Kish n_eff | **Holds.** No local Kish formula in `modules/brand` (grep for `sum(w)^2`-style expressions: only DoA and constellation maths unrelated to n_eff). Both ways: `test_kish_effective_n.R` against main's `03b/09e/11` (branch `00_guard.R`): 8 failures and errors (no `n_effective` column, demographics interval does not widen, extension p-value does not rise); on the branch FAIL 0, PASS 30. Summary-row builder consistent (`base = avg_pct * avg_n`, `total_n = avg_n`). Funnel closures dead in production: `00_main.R:1147` passes `sig_tester = NULL`; `run_significance_tests()` returns the empty frame when the tester is NULL (`03b:375-378`); `03c`/`03d` read the empty frame, nothing renders. Audience Lens: `n_eff` travels and the classifier uses it (`13c:175-176`); F8 was the side effect. | `scratchpad/review/h2/test_kish_main.R` |
| 4 | H1 weighted category penetration | **Holds.** Both ways: the three H1 tests against main's `09h/09c/09d`: 5 failures (unweighted 5/8 where the weighted 104/107 is expected); on the branch FAIL 0, PASS 127. Unweighted counts travel: `n_buyers_uw` / `total_n_uw` in the overview record (`09h:320-321`), rendered as "n 498 of 1,200" under the R table and present in the JS island. Awareness cells beside them are weighted (`09h:295`). | `scratchpad/review/h1/test_h1_main.R` |
| 5 | Branded Reach wrapper self-call | **Holds.** Main's `10_branded_reach.R` sourced with the branch's `10a/10b/10c`: `run_branded_reach(data, structure = list(marketing_reach, reach_media), ...)` errors `unused arguments (asset_list = asset_list, media_list = media_list)`. Branch: PASS, 1 ad, misattribution table AD1, shares sum to 1. Orchestrator passes `structure = structure` (`00_main.R:820-822`), so the wrapper is the production entry. `test_branded_reach.R` FAIL 0 PASS 72; `test_integration.R` (with the `reach_values` fixtures) FAIL 0 PASS 44 SKIP 1. | `scratchpad/review/br/probe.R` |
| 6 | M3 island escaping | **Holds, executed.** A scratch copy of the IPK fixture with one BrandLabel set to `Bad </script><!--<script>alert(1)</script> "q" \ end` ran through `run_brand()` (PASS) and the generator (PASS). Raw HTML: 12 real islands (14 occurrences of the open tag, two of them inside JS comments), zero islands containing `</script`, `<!--` or `<script`; the label is stored as `Bad <\/script>...`. Chrome: 12 islands found, 12 `JSON.parse` OK, 0 failed, 10 carry the hostile label, the MA table rendered, and the label shows in page text. `.pfo_escape_json()` now delegates to the shared escape. Static test `test_json_islands.R` scans every site. | `scratchpad/review/m3/`, `m3b/` |
| 7 | M1, M8, M2, H5, M9 and the GUI | **Holds at read level; Shiny not executed by anyone.** `brand_gui_outcome()` called after both generators (`run_brand_gui.R:347`); its level drives the console banner (384-392), the notification type and duration (405-407) and Step 5's warning list (454, 473). A PARTIAL engine result keeps `success = TRUE` but the headline says "completed with N warning(s)"; a generator refusal is "was not written: reason" (unit tests, `test_gui_outcome.R` FAIL 0 PASS 43 after F4's addition). `%||%` is defined by `00_guard.R:822`, which the GUI sources through `00_main.R`. M1: shape guard wired at `00_main.R:276-279`; integration test refuses a raw export with `DATA_NO_ALCHEMER_PARSER_OUTPUT`. M8: F3. M9: the package-guard return is unreachable in practice (F19). The GUI file parses. | Read; `test_gui_outcome.R` |
| 8 | Loop-level `<<-` | **Holds.** 22 `warnings_list <<-` remain in `00_main.R`, each inside an `error = function(e)` handler. | Read, 10 lines of context per site |
| 9 | M4 gate, one basis change, `suppress_base = 0` | **Holds, with F7.** Nine sites go through `.brand_meets_min_base()`; only the DoP-awareness row gate changed basis (`n_aware_w` to `n_aware_uw`, `09b_dop:303`). `suppress_base = 0`: `meets_min_base()` is `base >= min_base`, NA caught first by the `no_data` branch. Threshold typing: `meets_min_base()` stops on a non-numeric threshold; `01_config.R:179` coerces `portfolio_min_base` to integer, funnel thresholds are coerced twice; a direct call with `"30"` errors on the branch where main compared strings silently (`5 < "30"` is FALSE). Production reaches neither. | Read; executed probe |
| 10 | M7 wording and em dashes | **Holds after F5, F11, F12.** Checked against the engine: MA MPen denominator is all respondents, weighted (`calculate_mpen`: `sum(weights * linked) / sum(weights)`; base row n = 438 for DSS); norms caption: penetration over all category respondents (`08c` `w_sum`), buyers = `m_vec > 0`; overview: category usage weighted over all respondents (`09h:307-308`), "% who bought" unweighted from `pen_mat_raw` (`09h:62-63,115`); demographics: within-option known base, weighted (`11:270-274`), "Show counts" toggle exists (`brand_demographics_panel.js:130`); branded reach: `n_eligible`/`n_seen` are weighted sums (`10a:143-144`); notes block values match the engine (category usage 63.3 = 760/1200; norms penetration 0.890 = 390/438). Em dashes on added lines: all inside the cherry-picked review doc, one retained fragment in the dead `cb_norms_table_html()`, and the JS glyph (F12, fixed). | Read; RDS and HTML greps |
| 11 | Config hygiene | **Holds in code, not in the shipped binaries (F10).** `db_importance_method`: no code hit; only docs recording its removal. Templates generated to a temp dir and loaded by `test_config_templates.R` (FAIL 0 PASS 62, my run), which covers the three MarketingReach columns and the `all` / `non_buyers` dropdown; `guard_validate_brand_config()` refuses `buyers` (`test_guard_and_config.R`, in the full run). The generator saves through `turas_saveWorkbook` (pre-existing, `generate_config_templates.R:37-67`). Six keys documented; three corrected (F9). | Executed tests; read |
| 12 | Scope | **Holds.** | `git diff --name-only 3f85abb3..HEAD` |
| Suites | 0 failures at the tip | **Holds.** 2360 / 0 / 2 / 1 at `7c7a13ae`; 2399 / 0 / 2 / 1 at `e9cd638d`. | Section 1 |

## 4. Loader and sourcing checks

- `panels/00_json_island.R` is sourced first in production: `99_html_report_main.R:96-100` uses `sort(list.files())`. No new file under `modules/brand/R/`, so `.source_brand_module` is untouched.
- `.brand_source_shared()` finds `modules/shared/lib` from `TURAS_ROOT` (set by `launch_turas.R:54`; the GUI also `setwd`s to it, `run_brand_gui.R:38,312`) or a walk up from the working directory. Sixteen call sites, each inside an element `tryCatch` in `00_main.R`, so a missing shared library surfaces as a refused element with a console box.

## 5. What the tracks did and did not run

- Browser track: ran the C2 round trip, the Save path, the clear case, the M3 hostile-label report in Chrome and an island-site sweep. Its C2 and M3 results were reproduced by me above; F13's mechanism was confirmed by me, its behavioural consequence and F14 rest on its evidence.
- Engine track: ran Branded Reach both ways, H2 both ways, H1 both ways, the summary-row consistency numbers, the text-threshold probes, a weighted Audience Lens call (source of F8) and the `<<-` classification. All reproduced by me except the weighted Audience Lens call; F8 was then verified by the new test.
- Config track: generated and loaded the templates, read the GUI, walked the M7 strings, checked the guide and the em dashes. F3, F5, F9, F10, F11 came from it and were each confirmed by me against the code; F15, F17, F18, F19 are its reads.
- Nobody executed the Shiny server. Nobody ran a real client project. The `transform_brand_charts()` failure (F4) was surfaced, not fixed.

## 6. What Duncan must re-eyeball

1. Regenerate from `e9cd638d` or later. In Category Buying, Dry Seasonings: CHK and HND now flag over; the expected SCR column reads about two points lower across the board than what was eyeballed on 4 September.
2. The run will end PARTIAL with "HTML report: chart layer dropped, transform failed: missing value where TRUE/FALSE needed" until F4's underlying transform bug is fixed. That line was always true; it is now visible.
3. The DoP-awareness row gate now counts unweighted aware respondents (the notes flag this too).
4. Decide F3 (blank weights become 0 with a warning, or refuse) and F10 (which template workbooks are canonical).

## 7. Not done or not verified

- The Shiny GUI was not executed; the verdict helper is unit-tested and its use sites read.
- The "Chart transform failed" root cause was not investigated beyond confirming it is identical on main.
- No OneDrive project, no real client config, no merge, no push.
- Scratch artefacts for every executed check are under `/private/tmp/claude-501/-Users-duncan-Dev-Turas/fe6111fe-f7a9-4a81-9d57-f3563cbcf36a/scratchpad/review/` (session-local; not part of the repo).
